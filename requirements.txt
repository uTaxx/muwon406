# Streamlit Community Cloud 배포 전용 의존성 목록.
#
# 왜 pyproject.toml을 안 쓰고 이 파일을 두는가: Streamlit Cloud는
# pyproject.toml을 Poetry로 해석하는데, src/ 레이아웃(프로젝트명 muwon406 ≠
# 패키지명 muwon)에서 "현재 프로젝트를 패키지로 설치" 단계가 실패했다.
# 그걸 피하려고 [tool.poetry] 설정을 넣었더니 이번엔 [project] dependencies를
# 아예 안 읽어 의존성이 하나도 안 깔렸다. Streamlit Cloud는
# requirements.txt가 있으면 그걸 우선해서 pip으로 그대로 설치하므로,
# 배포 경로만 이 파일로 분리한다.
#
# 로컬 개발과 GitHub Actions는 이 파일을 쓰지 않는다 — 그쪽은 계속
# pyproject.toml(`pip install -e ".[dev,cloud]"`)을 쓴다. 따라서 의존성을
# 추가할 때는 pyproject.toml과 이 파일 양쪽을 같이 고쳐야 한다.
#
# 대시보드가 실제로 필요로 하는 것만 담는다 — 백테스트/ML/실시간 웹소켓
# 관련 패키지는 대시보드가 쓰지 않으므로 뺐다(설치 시간 단축).

# 코어 (muwon 패키지가 import 하는 것들)
pandas>=2.2
numpy>=1.26
requests>=2.31
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
SQLAlchemy>=2.0
APScheduler>=3.10
python-telegram-bot>=21.0
ta>=0.11.0
loguru>=0.7
cryptography>=42.0

# 대시보드 UI
streamlit>=1.35

# 구글드라이브 상태 동기화 (muwon/cloud/gdrive_sync.py)
google-api-python-client>=2.100
google-auth>=2.23
