# 인수인계 — 현재 상태와 다음 할 일

최종 갱신: 2026-09-08

---

## 1. 지금 어떤 상태인가

### 대시보드 (`public/lxgroup-mi/hub.html`)

주소: <https://utaxx.github.io/muwon406/lxgroup-mi/hub.html>

메뉴 구조가 **계열사 중심**으로 바뀌었다.

```
헤더 버튼:  키워드 신청/변경 · AI 학습 데이터 · 대시보드 설정 · 사용 매뉴얼

왼쪽 메뉴:  Home
            키워드 뉴스
            특정 키워드 검색
            계열사 현황        ← 그룹 실적 + (계열사 선택 시) 경쟁사 성과 + 리스크 관리
            시장 지표          ← 계열사 줄을 공유하는 별도 메뉴
            자본시장 딜 현황   ← [주요 공시] DART + [관련 보도] 자본시장 계열 뉴스

계열사 화면 = 왼쪽 본문(그룹 실적 / 경쟁사 성과 + 리스크 관리)
              + 오른쪽 계열사 줄 [그룹 전체][인터][판토스][하우시스][글라스][세미콘][MMA][홀딩스]
              (처음 열리는 것은 「그룹 전체」 = 그룹 실적)
```

- 계열사 선택은 **하나만** 있다(`coTab`). 경쟁사와 시장 지표가 이 값을 공유한다.
  계열사 화면에서 실제로 켜는 칸은 `coPick`이고(`그룹` 또는 계열사 이름),
  계열사를 고르면 `coTab`도 같이 따라간다.
- 이름을 「계열사 들여다보기」에서 **「계열사 현황」**으로 바꿨다(2026-09-07).
- **그룹 실적**(`co-group`)이 기본 진입 탭이고, 이 탭만 계열사를 가리지 않는다. `경쟁사` 시트에서 `우리편`인
  줄만 모아 `compBlocks(것들, '그룹')`로 그린다 — 블록 렌더러는 경쟁사와 같고,
  열쇠 앞에 `그룹`을 붙여 펼침·기간·보기 상태만 따로 둔다. 이 탭에서는
  `#coTabs`(계열사 선택 줄)를 감춘다.
- 경쟁사 성과에서 **「우리」 배지와 붉은 띠를 없앴다**(2026-09-07). 회사 정렬도
  매출 규모 순 하나로 바꿨다. 우리 회사만 보는 자리는 그룹 실적 탭이다.
- 공통 지표(환율·금리 등)는 계열사를 바꿔도 시장 지표 탭 아래쪽에 항상 표시된다.
- 마지막에 본 계열사와 하위 탭은 localStorage에 저장된다
  (`lx-co` · `lx-co-pick` · `lx-deal-pick`).
- **탭을 옮길 때 주소에 `?tab=` 을 써 넣지 않는다**(2026-09-08). 써 넣으면
  주소창을 복사해 건네준 링크가 「그 사람이 마지막에 보던 탭」으로 열린다 —
  링크를 받은 사람은 Home 부터 보고 싶어 한다. 주소에 `?tab=` 이 적혀 들어온
  경우(사용설명서의 바로가기)는 그대로 그 탭을 연다. 같은 까닭으로 옛 주소
  스텁(`index.html`·`articles.html`)도 `?tab=apply`·`?tab=search` 로 보내지 않고
  Home 으로 보낸다.
- 구 주소 `?tab=comp`, `?tab=risk`는 계열사 화면으로 연결된다. `?tab=index`는
  이제 시장 지표가 왼쪽 메뉴에 있으므로 그대로 그 메뉴다. `?co=글라스`로 계열사를
  직접 지정할 수 있다.

### 첫 화면(Home)

카드는 모두 `#tab-home`의 직계 자식이고, `설정보이기()`가 `appendChild`로 사용자가
정한 순서대로 다시 배치한다. 기본 순서:

1. **공통 지표** — 환율·금리 등. 아침에 제일 먼저 보는 값이라 맨 위
2. **주요 뉴스** — 가장 최근 수집분 중 메일 발송 기준을 통과한 기사.
   **총점(중요도+관련성+긴급성, 3~15) 내림차순**으로 세우고 기본 5건만 펼친 뒤
   나머지는 「나머지 N건 더보기」로 접는다. 총점이 같으면 최신 기사가 앞
   (`Array.sort`가 안정 정렬이라 날짜순 배열을 총점으로 다시 세우면 자동으로 그렇게 된다)
3. 계열사 카드 (경쟁사 n곳 · 지표 n개 · 최근 7일 기사 n건 + 변동폭 최대 지표 1개)
4. **자본시장 딜**(절반 폭) — 지분 거래·합병분할·영업자산·계열사경쟁사 네 숫자 + 다섯 줄.
   다섯 줄은 계열사·경쟁사가 낀 딜부터 채우고 남는 자리를 최근 M&A로 메운다
5. **자본시장 관련 보도**(절반 폭) — 딜 카드 바로 옆. 그룹 이름에 「자본시장」이
   든 기사를 최신순 다섯 줄. 공시는 확정된 사실이라 정확한 대신 늦고 기사는
   빠른 대신 「검토 중이라고 한다」까지라, 둘을 나란히 두면 「기사에는 났는데
   공시는 아직」인 건이 한눈에 보인다. 딜 화면의 관련 보도와 같은 `딜뉴스대상()`
   을 쓴다 — 두 자리가 다른 셈을 하면 반드시 어긋난다.
   이 짝만 `align-self:stretch` 로 높이를 맞춘다(`#tab-home` 은 `align-items:start`
   라 그냥 두면 짧은 쪽이 위로 붙어 어긋난다)
6. 그룹별 수집 현황 · 7. 실리코시스 요약 — 기본이 2열이라 넓은 화면에서 나란히 선다

카드 머리의 「전체 딜 →」·「전체 보도 →」는 `data-goto-deal` 로 딜 화면의 어느
하위 탭을 열지까지 들고 간다.

**카드 폭**도 설정이 정한다. `설정보이기()`가 `data-col`(1=한 줄 전체, 2=절반)을 찍고
CSS 는 `#tab-home > [data-col="2"]{grid-column:auto}` 하나로 처리한다. 2열 카드가
연달아 오면 그리드 자동 배치가 알아서 짝을 짓는다.

카드 안쪽 격자(`.hi` 지표 칸, `.co-grid` 계열사 카드)는 화면 폭이 아니라
`repeat(auto-fit, minmax(...))`로 **카드가 실제로 가진 폭**에 맞춘다. 화면 폭으로
칸 수를 정하면 절반 폭 카드에서 글자가 잘린다.

KPI 4개(마지막 수집 / 최근 7일 / 중요도 4↑ / 실리코시스 30일)는 **주요 뉴스 카드
머리(`.mail-head`) 안**, 「전체 기사 →」 왼쪽에 `.kpi-mini`로 작게 붙는다. 카드 안에
들어 있어서 순서 목록(`c.순서`)에는 넣지 않고 on/off만 따로 둔다(`CFG_KPI`). 이 때문에
`설정보이기()`의 재배치는 `:scope > [data-card=...]`로 직계 자식만 집는다 — 그냥
`querySelector`로 집으면 요약 숫자가 카드 밖으로 끌려 나온다.

`#tab-home`은 CSS 그리드다. `display:grid`를 `#tab-home.active`에만 거는 이유는
`.tab-panel{display:none}`보다 id 선택자가 세서, `#tab-home{display:grid}`로 두면 다른
탭에 있어도 첫 화면이 보이기 때문이다.

### 대시보드 설정

헤더의 「대시보드 설정」. localStorage(`lx-cfg`)에만 저장하며 서버로 전송하지 않는다.

- **첫 화면 구성** — 카드 6개를 체크로 on/off, ▲▼로 순서 변경, 「1열/2열」 버튼으로
  폭 변경 (`설정순서(통, 사전, 차례, 그릇, 폭그릇, 뒤에)` — `폭그릇`이 null 이면
  폭 버튼을 그리지 않는다. 계열사 하위 탭이 그 경우다)
- 요약 숫자 4개 on/off (`#cfgKpi`, 순서 변경 대상이 아님)
- 주요 뉴스 기준(중요도/관련성/긴급성)
- 주요 뉴스 표시 방식 — 먼저 펼칠 건수(기본 5), 최대 건수(기본 15),
  정렬(`score` 총점순 / `date` 최신순)
- **계열사 화면 구성** — 하위 탭 4개 on/off + 순서 변경. 맨 위 탭이 기본 진입 탭
- 표시할 계열사 on/off
- 기본값 복원 버튼

**저장 버튼은 없다.** 값이 바뀔 때마다 `설정저장()`이 곧바로 localStorage 에 쓴다.
대신 화면 아래에 붙는 `.cfg-bar`(sticky)에 마지막 저장 시각을 적는다. `설정저장()`은
`기억()`을 쓰지 않고 `localStorage.setItem`을 직접 호출한다 — `기억()`이 실패를
삼켜서 성공 여부를 알 수 없기 때문이다. 실패하면(시크릿 모드 등) 띠가 붉게 바뀌며
창을 닫으면 설정이 사라진다고 알린다.

저장 형태:
```
{ 순서:['ind','mail','co','deal','groups','risk'], home:{...,kpi},
  폭:{mail:1,co:1,ind:1,deal:1,groups:2,risk:2},
  판:2, 하위순서:['group','index','comp','risk'], sub:{...}, 계열사:{...},
  기준:{중요도,관련성,긴급성,손댐,읽었나}, 뉴스수:15, 첫뉴스수:5, 정렬:'score' }
```
`순서맞추기()`가 저장된 배열에서 모르는 이름을 버리고 빠진 것을 기본 순서대로 뒤에
붙인다. `폭맞추기()`는 1도 2도 아닌 값을 기본으로 되돌린다.

**`CFG_판`**: 기본 차례를 바꿔도 이미 쓰던 사람의 저장값이 이기므로 새 기본값이 닿지
않는다. 판을 올리면 `판올리기()`가 저장된 `하위순서`와 `lx-co-sub`만 한 번 버린다.
`coSub`가 스크립트 위쪽에서 곧바로 정해지므로 `설정()` 안이 아니라 그보다 앞에서 돈다.
카드 순서·폭·뉴스 기준은 사람이 정한 것이라 건드리지 않는다. 둘 다 있어서 카드를
새로 넣거나 없앤 뒤에도 옛 설정이 그대로 동작한다.

**카드를 새로 넣는 법**: ① `#tab-home` 안에 `data-card="키"`를 단 `<section class="card">`
추가 ② `CFG_HOME`에 한 줄 ③ `CFG_기본.순서`·`CFG_기본.home`·`CFG_기본.폭`에 그 키 추가
④ `renderHome()`에서 그리는 함수 호출. 저장돼 있던 옛 설정에는 없는 키라 순서 맨 뒤에
기본 폭으로 붙는다.

기준값은 신청/변경 화면을 한 번 열면 그 계정 설정을 가져다 쓴다. 사용자가 설정에서
직접 조정하면 그 뒤로는 덮어쓰지 않는다(`기준.손댐` 플래그).

### 엑셀 다운로드

브라우저에서 xlsx를 생성하던 코드(127줄)를 제거했다. 이제 `LXGroup_원본내려받기`
웹훅이 구글드라이브의 마스터 스프레드시트를 xlsx로 export해 그대로 반환한다.

- 시장 지표: 계열사용 1개 + 공통용 1개, 버튼 2개
- 경쟁사: 회사 카드마다 1개

보안: 요청받은 이름을 그대로 드라이브에 조회하지 않는다. `경쟁사` 시트에
`사용=TRUE`인 회사와 화면의 지표 탭 7개만 허용하고, 조회 범위도 해당 폴더로 제한한다.

---

## 2. n8n 워크플로우 (sondullab.app.n8n.cloud)

| 워크플로우 | ID | 주기 | 역할 |
|---|---|---|---|
| `LXGroup_지표조회` | `YpCv6bPYsEcxE4cS` | 웹훅 `lxgroup-지표` | 시장지표 + **리스크 등록표** 반환 |
| `LXGroup_지표_마스터정리` | `uhivQ1m5EK9NkwA4` | 6시간 | 「시장 지표」 폴더에 탭별 마스터 7개 갱신 |
| `LXGroup_원본내려받기` | `l1YMWL5bmAZUciMZ` | 웹훅 `lxgroup-원본` | 드라이브 마스터를 xlsx로 반환 |
| `LXGroup_경쟁사_마스터정리` | `QHKK4TbrDL4yviwd` | 월 07:00 | 회사별 「실적 마스터」 4개 탭 갱신 |
| `LXGroup_경쟁사_조회` | `66OHWRcUSok5i0J0` | 웹훅 | 경쟁사 실적 반환 (+ 「전사 보태기」) |
| `LXGroup_운임수집` | `v0gAuNStBcRjUMgI` | 월 15:00 | KCCI 주간 운임을 시장지표값에 쌓는다 |
| `LXGroup_딜공시수집` | `Z6oFKQA3fLwOD9rU` | 매일 08:00 | DART 주요사항보고서에서 딜만 골라 「딜공시」에 쌓는다 |
| `LXGroup_딜조회` | `v2YFEGynGuz0bp6g` | 웹훅 `lxgroup-딜` | 「딜공시」를 최신순으로 반환 + 계열사·경쟁사 표시 |
| `LXGroup_회사공시수집` | `H03ChiOEQZhutaL5` | 금 08:45 | 경쟁사 시트의 회사들이 낸 DART 공시 전부를 corp_code 로 훑어 「회사공시」에 쌓기 |
| `LXGroup_회사공시조회` | `8dqSPosl4vVDYKUQ` | 웹훅 `lxgroup-회사공시` | 계열사를 받아 그 계열사·경쟁사 공시만 최신순으로 반환 |
| `LXGroup_경쟁사_실적추출` | `MHGVBh9mKGzzUobP` | 월 04:10 | 원문에서 부문 실적 + **「요약 손익 되줍기」**(신규) |
| `LXGroup_경쟁사_원문저장` | `POiRWyDv6l0a33Tu` | 월 03:10 | DART 원문을 드라이브에 저장 |
| `LXGroup_경쟁사_비용추출` | `Qa9mF8rLJd8d28Il` | 월 05:10 | 주석에서 성격별·판관비 뽑기 |
| `LXGroup_경쟁사_부문매핑마스터` | `wxCmHixoobM6inyF` | 월 06:10 | 새로 나타난 부문을 매핑표에 보태기 |
| `LXGroup_경쟁사_공시수집` | `M8QuMCtINJ2GN7jx` | 매일 아침 | 공시 목록만 원장에 쌓기(원문은 안 받음) |

그 외 기존 워크플로우(공시수집, 원문저장, 실적추출, 비용추출, 부문매핑마스터,
MI, MI_Sub, 신청접수, 콜백처리, 실패알림)는 변경 없음.

### 조회 웹훅의 식별코드 확인 (2026-09-07)

딜 화면을 붙이면서 조회 웹훅들을 하나씩 두드려 봤더니 **둘이 코드를 보지 않고 있었다.**

| 웹훅 | 고치기 전 | 고친 뒤 |
|---|---|---|
| `lxgroup-경쟁사` | `ZZZZ`·`AAAA`·빈 값에도 82,687바이트 전체 반환 | 404 |
| `lxgroup-cases` | 아무 코드에나 소송사건 원장 5건(평결액·책임배분·요약) 전체 반환 | 404 |

둘 다 지표조회와 같은 모양으로 막았다 — 웹훅 바로 뒤에 `코드 확인`(대문자화) →
`설정 읽기`(식별코드로 걸러 읽기, `alwaysOutputData`) → `쓸 수 있는 코드인가`
(식별코드가 비어 있지 않고 상태가 `운영중`) 를 두고, 아니면 404 로 답한다.
`경쟁사_조회`는 「설정」 시트를 읽게 되면서 성공 실행 데이터를 남기지 않도록 바꿨다.

`lxgroup-cases`는 노드가 116개인 `LXGroup_신청접수` 안에 있어 사건 갈래
(`Webhook7 → 사건시트조회 → 사건목록조립 → Respond to Webhook8`)만 오른쪽으로
밀고 그 앞에 세 노드를 끼웠다. 화면 쪽은 404 를 받아도 `cases: []` 로 읽어
빈 목록을 그리므로 고칠 것이 없었다.

나머지 조회 웹훅은 이미 확인하고 있었다 — `lxgroup-lookup`(빈 응답),
`lxgroup-articles`(«등록된 내역을 찾을 수 없습니다»), `lxgroup-knowledge`(빈 응답),
`lxgroup-지표`·`lxgroup-딜`(404), `lxgroup-api-cost`(비밀번호). 쓰기·수집을 일으키는
웹훅(intake·instant·adhoc-search·feedback·delete-articles·case-status)은 두드리지
않았다 — 두드리는 것 자체가 수집을 돌리거나 줄을 지운다.

### n8n 실행 한도 — 소진(2026-09-07 12:02 KST)과 그 뒤 조치

**그날 무슨 일이 있었나**: 모든 웹훅이 `HTTP 200 · 본문 0바이트`로 돌아오고 대시보드가
어느 탭에서도 「불러오지 못했습니다」를 띄웠다. 실행 기록에는 웹훅이 첫 노드에서 바로
죽으며 `Execution limit reached. Consider upgrading your plan` 이 남았다. 코드 문제가
아니라 **n8n Cloud 요금제의 월 실행 건수를 다 쓴 것**이고, 예약 워크플로우도 같이 멈춘다.

**실측한 하루 실행 수**(KST 기준, 남아 있는 실행 기록으로 셈)

| 날짜 | 실행 수 |
|---|---|
| 9/4 (목) | 527 |
| 9/5 (금) | 547 |
| 9/6 (토) | 588 |
| 9/7 (월, 12:56까지) | 421 (한도 소진 뒤 실패분 포함) |

**일평균 약 550건 · 월 약 16,500건.** 그중 480건(87%)이 계열사 수집기 넷이었고,
그중 432건이 10분마다 도는 셋이었다. 새 정기보고서는 한 주에 몇 건뿐이라 그 432번의
대부분은 「읽을 게 없다」를 확인하고 끝났다.

### 한도 복구 뒤 되돌린 것 (2026-09-08)

한도를 올린 뒤 확인한 결과와 조치.

- **모든 조회 웹훅 정상**(지표 76KB · 경쟁사 84KB · 딜 177KB · 사건 8KB, 잘못된 코드는 404).
- **LX판토스·LX글라스 완료**. 되읽기로 표시한 10건이 한도가 걸리기 전 세 번의 실행에서
  전부 처리돼 있었다. 실적추출·원문저장을 손으로 한 번씩 더 돌렸으나 할 일이 없었다
  (백로그 0). 그룹 실적에 일곱 계열사 중 **여섯**이 숫자와 함께 뜬다
  (LX홀딩스는 `사용=FALSE` 로 의도적으로 빠져 있다).
- **한도 소진 중 실패한 예약 작업을 손으로 되돌렸다** — `LXGroup_딜공시수집`(9/7·9/8분
  9건 신규), `LXGroup_지표수집`, `LXGroup_경쟁사_공시수집`. 셋 다 시트에만 쓴다.
- **아직 안 돌린 것: `LXGroup_MI` 뉴스 발송.** 9/7 16:00 과 9/8 07:00 발송이 실패했다.
  손으로 돌리면 **수신자 다섯 명에게 실제로 메일과 텔레그램이 나가므로** 사용자 확인
  없이는 돌리지 않는다. 11:00 발송은 예정대로 자동으로 나간다.

### 딜공시수집이 날마다 60일을 다시 받던 문제 (2026-09-08 고침)

되받는 기준이 「구분마다 60일 경계보다 오래된 줄이 있는가」였는데, 지분처분처럼 드문
구분은 경계 근처 이틀에 한 건도 없어 **조건이 날마다 거짓**이 됐다. 그래서 매일 60일치
열다섯 쪽을 다시 받았다(실행 수는 1건이라 한도에는 영향이 없지만 DART 호출과 실행
시간이 낭비 — 16초).

**원장의 가장 오래된 접수일이 60일 경계보다 오래되면 14일만 받는다**로 바꿨다. 첫 수집
때 60일을 채워 두면 그 뒤로는 경계가 날마다 밀리므로 조건이 계속 참이다. 쪽 수도
일수에 맞춰 14일이면 여덟 쪽만 만든다. 실측 16초 → 6.7초, 열다섯 쪽 → 네 쪽.

분류 규칙을 고쳐 예전에 버린 공시를 다시 봐야 할 때는 **「딜공시」 시트를 머리줄만 남기고
비운 뒤 한 번 돌린다**. 자동으로 알아채게 만들려던 앞의 규칙이 오히려 매일 헛일을 시켰다.

### 수집 주기를 금요일로 모으고 손으로 돌릴 길을 냈다 (2026-09-08)

**왜**: 「쓸데없는 실행이 안 생기게, 필요하면 내가 눌러서 돌린다」는 방침.

**자동 주기** — 모두 `Asia/Seoul` 로 못 박고 앞뒤 순서를 지킨다.

| 시각(금) | 워크플로우 | 전 |
|---|---|---|
| 02:10 | `LXGroup_경쟁사_공시수집` | 매일 아침 7시 |
| 03:10 | `LXGroup_경쟁사_원문저장` | 월 03:10 |
| 04:10 | `LXGroup_경쟁사_실적추출` | 월 04:10 |
| 05:10 | `LXGroup_경쟁사_비용추출` | 월 05:10 |
| 06:10 | `LXGroup_경쟁사_부문매핑마스터` | 월 06:10 |
| 07:00 | `LXGroup_경쟁사_마스터정리` | 월 07:00 |
| 07:30 | `LXGroup_운임수집` | 월 15:00 |
| 08:00 | `LXGroup_지표수집` | 매일 아침 8시 |
| 08:30 | `LXGroup_딜공시수집` | 매일 아침 8시 |
| 09:00 | `LXGroup_지표_마스터정리` | 6시간마다 |

키워드 뉴스(`LXGroup_MI`)는 **하루 3회 그대로**다.

**손으로 돌리기** — `LXGroup_수동실행`(`rasNV9fNhONkZTOr`), 웹훅 `lxgroup-실행`.

- 받는 것: `{ code, pw, 대상 }`. 대상은 `지표` 또는 `경쟁사` **둘뿐**이고, 모르는 값은
  빈 값으로 바꿔 거절한다 — 시키지 않은 워크플로를 부르는 길을 만들지 않는다.
- 확인: 「설정」 시트의 식별코드·상태(`운영중`)와 **`실행 PW`**(W열, 2026-09-08 신설).
  **값이 비어 있으면 무조건 거부**한다(fail-closed). 비교는 상수 시간이라 응답 시간으로
  길이가 새지 않는다. 비용조회 인증과 같은 방식이고, 기사 삭제용 관리자 PW 와는 다른 값이다.
- 부르는 것:
  - `지표` → 운임수집 → 지표수집
  - `경쟁사` → 공시수집 → 원문저장 → 실적추출
- **드라이브 마스터 파일 갱신(마스터정리·부문매핑·지표 마스터정리)은 부르지 않는다.**
  화면이 읽는 것은 「시장지표값」·「경쟁사실적」 시트뿐이고, 마스터 파일은 「엑셀 내려받기」에만
  쓰인다. 뺀 덕분에 수동 실행이 몇 분 → 1~2분으로 줄었다(마스터정리 혼자 80~230초).
- **응답을 먼저 보내고 뒤에서 돈다.** 화면이 몇 분을 기다리지 않게 하려는 것이다.
  그래서 「시작했습니다, 몇 분 뒤 새로고침하세요」라고만 알린다.
- 한 단계가 실패해도 뒤는 돈다(`onError: continueRegularOutput`). 못 채운 것은 다음
  금요일이나 다시 누르기로 낫는다.
- 성공 실행 데이터는 남기지 않는다 — 「설정」 시트를 읽으므로 실행 PW 가 기록에 쌓인다.

**부른 쪽이 되려면** 대상 워크플로 다섯에 `Execute Workflow Trigger`(「손으로 부를 때」)를
붙였다. 예약 트리거는 그대로 두었으므로 두 길이 같이 산다.

**화면**: 시장 지표·그룹 실적·경쟁사 성과 세 곳에 「↻ 업데이트」 버튼.
비밀번호는 창이 열려 있는 동안만 `sessionStorage` 에 남기고, 틀리면 지운다.
같은 대상 버튼이 두 화면에 있어 하나를 누르면 둘 다 20초 잠근다.
코드는 `identifierCode` 가 아니라 **`sharedCode()`** 에서 읽는다 — `identifierCode` 는
「키워드 신청/변경」을 거쳤을 때만 채워져서 계열사 화면만 연 사람에게는 빈 값이다.

**할 일**: 「설정」 시트 W열 `실행 PW` 에 값을 채워야 버튼이 동작한다. 지금은 비어 있어
누구든 거부된다(의도된 상태).

### 계열사 수집기를 주 1회로 (2026-09-07)

주기만 낮추면 안 된다 — 수집기는 한 번에 몇 건씩만 집는 구조라, 주 1회로 두면 시즌에
스물두 회사 것이 몰릴 때 몇 주씩 밀린다. **주기와 배치를 같이 바꿨다.**

| 워크플로우 | 전 | 후 | 한 번에 |
|---|---|---|---|
| `LXGroup_경쟁사_원문저장` | 10분 (144/일) | **월 03:10** | 20 → **25건** |
| `LXGroup_경쟁사_실적추출` | 10분 (144/일) | **월 04:10** | 4 → **20건** |
| `LXGroup_경쟁사_비용추출` | 10분 (144/일) | **월 05:10** | 5 → **12건** |
| `LXGroup_경쟁사_부문매핑마스터` | 6시간 (4/일) | **월 06:10** | — |
| `LXGroup_경쟁사_마스터정리` | 30분 (48/일) | **월 07:00** | — |

- 시각은 앞뒤 순서를 지킨다 — 원문을 받아야 실적을 뽑고, 실적이 들어와야 새 부문이
  나타나고, 그게 다 끝나야 마스터를 다시 쓴다. 다섯 워크플로우 모두 시간대를
  `Asia/Seoul` 로 못 박았다(안 박으면 인스턴스 기본 시간대를 따라 새벽이 아닌 때 돈다).
- 배치 크기를 회사 수(22)보다 크게 잡은 것은 원문저장뿐이다. **비용추출은 12로 멈췄다** —
  비용 주석은 표를 200개까지 훑어 건당 시간이 실적추출보다 길다. 시즌에 스물두 건이
  몰리면 두 주에 나눠 처리된다. 화면에 바로 보이는 값이 아니라 그 정도 지연은 괜찮다.
- `executionTimeout` 은 걸지 않았다. 마스터정리는 한 번에 80~230초가 걸려, 300초를
  걸어 두면 오히려 죽는다. 대신 다섯 개 모두 `errorWorkflow` 를 `LXGroup_실패알림`
  으로 붙여 주 1회 실행이 실패해도 텔레그램으로 알게 했다.
- **`LXGroup_경쟁사_공시수집`은 매일 아침 그대로 둔다.** 하루 1건이라 줄일 실익이 없고,
  매일 돌아야 원장이 최신이라 주간 수집기가 그것을 읽어 처리한다.

**효과**: 예약분 484건/일 → 5건/주(약 0.7건/일). 하루 총계 550 → **약 70건**,
월 16,500 → **약 2,100건**.

**되돌리려면** 각 워크플로우의 트리거 노드 이름이 곧 주기다(「주 1회 · 월 04:10」).
cron 을 `*/10 * * * *` 로 되돌리고 `한번에` 상수를 낮추면 된다.

**남아 있는 낭비**: `TMP_비용시트_보기`(`KjyXZC5kBqBvOulX`)가 아직 `active` 로 남아 있다.
웹훅이라 부르지 않으면 실행되지 않지만 정리 대상이다.

### 주의: n8n MCP `addNode`의 함정

`addNode`의 스키마에 `executeOnce`, `alwaysOutputData`가 **없어서 조용히 버려진다.**
이걸 모르고 지표조회에 시트 읽기 노드를 추가했다가, 상위 노드의 2,000개 아이템마다
시트를 한 번씩 읽어 120초 타임아웃에 걸렸고 **지표 조회가 약 4분간 중단됐다.**

→ 노드 추가 후 반드시 `setNodeSettings`로 `executeOnce`를 지정하고,
  **publish 전에 `execute_workflow`(manual)로 draft를 실행해 확인한다.**

### 시장 지표 — 수기 주간 항목 반영 (2026-09-07)

사용자가 매주 손으로 정리하던 8개 항목을 공신력 있는 무료 출처로 옮겼다.

| 수기 항목 | 처리 | 출처 |
|---|---|---|
| 환율 | 이미 있음 | ECOS (원/달러·원/위안, 일별) |
| 유가 브렌트 $/bbl | **추가** `OIL_BRENT` | FRED `DCOILBRENTEU` (EIA, 일별) |
| 석탄 ICI4·ICI5 | **대체** `COAL_NEWC` | FRED `PCOALAUUSDM` (IMF 호주 뉴캐슬, 월별) |
| GAM 판가 | 불가 | 자사 광산 판매가라 공개 데이터가 없다 |
| 운임 SCFI | **대체** `FRT_KCCI` | 한국해양진흥공사 KCCI 종합 (주간, pt) |
| 운임 북미서안 | **추가** `FRT_USWC` | KCCI `KUWI` (주간, $/FEU) |
| 운임 유럽향 | **추가** `FRT_EUR` | KCCI `KNEI` (주간, $/FEU) |
| MMA 국제가 $/t | 이미 있음(근사) | 관세청 MMA 수출·수입단가 (월별) |

- **ICI4/ICI5**(Coalindo·Argus), **SCFI**(상하이해운거래소), **MMA 스팟**(ICIS·Argus)은
  모두 유료 구독이라 무료·공신력 있는 대체가 없다. 뉴캐슬탄은 발열량이 달라
  ICI 대신이 아니라 방향 참고용이고, 지표 설명에도 그렇게 적어 두었다.
- **KCCI**는 부산 기점 13개 항로라 SCFI(상하이 기점)보다 우리 항로에 가깝다.
  주소에 `sDay`·`eDay`를 붙이지 않으면 최근 7일치만 오므로 2022-01-01부터 받는다.
  2022-11-07 이후 191주가 한 번에 들어온다. 페이지에 `(단위 : pt, $/FEU)`로 적혀 있다.
- 값은 `시장지표값` 시트에 `주기='일'`로 쌓는다. 조회 웹훅이 `'일'`만 일별 통에
  넣기 때문이다(`'주'`로 쌓으면 월별로 흘러가 미완성 달 규칙에 걸린다).
- 공통 지표가 8개에서 11개가 되어 첫 화면 공통 카드 상한을 8 → 12로 올렸다.

### 자본시장 딜 (2026-09-07 신설)

「자본시장의 주요 딜 현황을 관리하는 대시보드」 요청에 대한 답이다. 두 갈래로 붙였다.

**① 키워드 보강** — `LXHOLDINGS` 설정에 그룹 **「자본시장_딜」**을 추가했다(전체 9개).
기존 「자본시장」 그룹 834건 중 딜 단계를 말하는 기사는 4건(0.5%)뿐이었고, 그룹
키워드는 `M&A(483) · 경영권(178) · 지주사(104) · 인수합병(50) · 지주회사(17) · 볼트온(2)`로
시장 논평 쪽에 쏠려 있었다. 새 그룹은 **단계를 나타내는 말**만 잡는다 —
`예비입찰 · 본입찰 · 우선협상대상자+매각 · 매각주관사 · 인수의향서 · 스토킹호스 ·
공개매각 · 주식매매계약 · 숏리스트+인수 · 매각 본계약`. 경영권 매각/인수류는 기존
「자본시장」의 `경영권`이 이미 잡으므로 넣지 않았다.

**② DART 주요사항보고서 수집** — `LXGroup_딜공시수집`이 매일 08:00에
`opendart.fss.or.kr/api/list.json`을 **corp_code 없이 기간+`pblntf_ty=B`로** 호출해
시장 전체를 훑고, 보고서명으로 딜만 골라 「딜공시」 시트에 쌓는다.
컬럼: `접수번호 · 접수일 · 회사명 · corp_code · 시장 · 보고서명 · 구분 · 링크 · 수집일시`.

- 구분 6가지: `지분취득`(타법인주식…취득/양수결정) · `지분처분`(…처분/양도결정) ·
  `합병·분할` · `영업·자산 양수도` · `유상증자` · `메자닌`.
  **DART는 지분 거래를 「양수/양도」로 적는다** — 처음에 「취득/처분」만 보다가 지분
  거래를 통째로 놓쳤다. 자기주식 취득은 딜이 아니라 「타법인」이 붙은 것만 본다.
- **되받는 기준**: 보통 14일치만 받고 접수번호로 중복을 거른다. 다만 **구분마다
  60일 경계보다 오래된 줄이 하나라도 있는지** 확인해서, 없으면 그 구분은 아직
  60일치를 못 채운 것이므로 60일을 다시 훑는다. 줄 수만 세는 규칙으로 두었더니
  분류를 고친 뒤에도 되받지 않아 지분 거래만 14일치로 남았었다.
- 2026-09-07 첫 적재 결과: **60일(07-09~09-07) 933건** —
  유상증자 429 · 메자닌 199 · 합병·분할 144 · 영업·자산 양수도 97 · 지분취득 39 · 지분처분 25.
  이 중 우리 계열사·경쟁사가 낸 것은 2건(금호석유화학 회사합병결정)이다.
- `LXGroup_딜조회`(웹훅 `lxgroup-딜`)가 「딜공시」와 「경쟁사」를 읽어 화면에 넘긴다.
  **corp_code로** 딜 주체가 우리 계열사인지 등록 경쟁사인지 가린다 — 회사명은
  「주식회사」·괄호·띄어쓰기가 제각각이라 이름으로 맞추면 반드시 어긋난다.
  응답 176KB(gzip 13.5KB)라 탭을 열 때만 부르지 않고 상황판에서도 배경으로 받는다.
- 화면 기본이 **M&A 네 구분**인 까닭은 건수다. 933건 중 628건이 유상증자·메자닌인데
  대개 코스닥 소형주의 자금 조달이라 전략 부서가 볼 것이 아니다. 구분 버튼에 건수를
  적어 두어 누르기 전에 볼 것이 있는지 알 수 있게 했다.

**남은 한계**: DART 공시는 **이사회를 지난 뒤**에 나온다. 「매각 검토」·「예비입찰」
단계는 기사에만 있으므로 ①과 ②를 같이 봐야 딜의 앞뒤가 맞는다. 거래 상대방·금액·
일정은 공시 원문에 있고 이 화면은 무엇이 언제 나왔는지까지만 보여 준다.

**미공개 중요정보는 넣지 않는다**: 식별코드는 사람이 외우는 짧은 문자열이고 웹훅
주소는 공개 저장소에 있다. **진행 중인 우리 회사 M&A 같은 미공개 중요정보는 이
시스템에 넣지 않는다.**

### 빠져 있던 실적을 채웠다 (2026-09-08)

「수집이 덜된 부분이 많다」는 지적에 원장(`경쟁사공시`)과 실적 시트(`경쟁사실적`)를
회사별로 대조해 봤다. 원인이 둘이었고 둘 다 고쳤다.

**① 엘엑스판토스가 2026년부터 사업보고서를 낸다** — `경쟁사` 시트의 `공시종류`가
「감사보고서」라 `LXGroup_경쟁사_공시수집`이 외부감사(`pblntf_ty=F`)만 조회했는데,
판토스는 2026-03-18 부터 정기보고서(`A`)로 사업보고서·분기·반기를 낸다. 그래서
2025 결산분부터 원장에 한 건도 안 들어왔다.

→ **회사마다 A와 F를 둘 다 조회한다.** 해당 없는 갈래는 `013`(조회된 자료 없음)으로
돌아오므로 그냥 넘어간다. `쓸것()` 판정도 갈래가 아니라 보고서명으로 바꿨다.
호출은 22 → 44건이 되지만 워크플로우 실행 횟수는 그대로 1이다(한 실행 안의 HTTP
호출은 실행 수에 안 잡힌다). 회사가 상장·비상장을 오가도 이제 따라간다.

**② 「부문표 못 찾음」 표시줄이 영구히 굳어 있었다** — 실적추출은 `경쟁사실적`에
접수번호가 있으면 그 보고서를 건너뛴다. 파서가 실패해 표시줄만 남긴 보고서는
파서를 고쳐도 다시 읽히지 않았다. 그래서 「요약 손익 되줍기」를 붙인 뒤에도
녹수·윈체·현대엘앤씨는 숫자가 하나도 없었다.

→ **표시줄에 파서 판 번호를 적는다.** 「부문 거르기」가 나가는 표시줄의 `단위`를
`부문표 못 찾음 v2` 로 바꾸고, 「읽을 보고서 고르기」는 **표시줄만 있고 그 번호가
지금 판과 다른** 보고서를 안 읽은 것으로 친다. 파서를 고칠 때 `판` 상수만 올리면
지난 실패분이 딱 한 번 더 읽힌다. 같은 판에서 또 실패하면 넘어가므로 무한히
되풀이하지 않는다. 두 곳의 상수가 같은 값이어야 한다.

**결과** (원문 5건 저장 + 재추출 3회 뒤. 실적 줄 1,714 → 1,777):

| 회사 | 전 | 후 |
|---|---|---|
| 현대엘앤씨 | 없음 | 2021~2025 |
| 녹수 | 없음 | 2021~2025 |
| 엘엑스판토스 | 2021~2024 (연간만) | 2021~2025 + 2023Q2~2026Q2 |
| 엘엑스엠엠에이 | 2021~2024 | 2021~**2025** |
| 금호석유화학·아나패스·SK네트웍스·포스코인터·어보브반도체·현대글로비스·벽산·이건창호 | 2025 없음 | **2025 추가** |

### 윈체와 CJ대한통운까지 마저 채웠다 (2026-09-08, 같은 날 이어서)

위에서 남겨 둔 셋을 마저 끝냈다. 원인이 셋 다 달랐다.

**① 윈체 — 주석 번호와 어긋난 칸 (파서 v3)**

윈체 손익계산서는 과목이 「I. 매출액(주석13)」이다. 「요약 손익 되줍기」의 이름
읽기는 괄호만 지워서 「매출액주석13」이 되고, 그러면 어느 이름과도 안 맞아 표가
통째로 버려졌다. 게다가 금액 앞에 빈 칸이 하나씩 있어 머리줄이 세 칸
(「과 목 | 제17(당)기 | 제16(전)기」)인데 값줄은 다섯 칸이다.

→ 두 가지를 고쳤다. **주로 시작하는 괄호를 통째로 떼고**, 열 번호로 맞추는 방식과
**머리줄의 기 이름·값줄의 숫자를 나온 차례대로 짝짓는 방식**을 둘 다 계산해 더 많이
건지는 쪽을 쓴다. 수가 같으면 예전 방식을 그대로 믿으므로 잘 되던 표는 안 건드린다.
n8n 밖에서 로컬 하네스로 윈체 표와 판토스 표를 넣어 회귀까지 확인한 뒤 올렸다.

결과: **윈체 2021~2025** (2025 441억/−11.9억, 2024 916.2억/93.3억). 덤으로
어보브반도체·포스코인터의 빠진 분기, 이건창호 2021 이 함께 들어왔다.

**② CJ대한통운 분기보고서 — 실패한 줄이 굳는 구조적 버그**

「받을 것 고르기」가 `처리` 칸이 **비어 있지 않으면 무조건 건너뛰었다.** 그래서 한 번
「실패」로 적힌 줄은 영원히 다시 받지 않는다. 이 건은 2026-09-03 에 드라이브 403 을
한 번 맞고 닷새를 굴렀다. 403 의 정체는 **구글 드라이브 API 분당 쿼터**
(`rateLimitExceeded`)로, 파일이나 권한 문제가 아니라 잠깐의 속도 제한이다.

→ **「저장」일 때만 건너뛴다.** 고치자마자 그 줄이 정상 저장됐다.

**③ CJ대한통운 [첨부정정]사업보고서 — DART에 원문이 없다**

DART `document.xml` 이 `status 014 파일이 존재하지 않습니다` 로 답한다. 목록에는
나오지만 첨부만 고친 재제출이라 본문 문서가 없다. ②를 고친 뒤로는 이런 줄을 매주
영원히 다시 시도하게 되므로 그대로 둘 수 없었다.

→ **정정공시는 애초에 고르지 않는다.** 원문저장·실적추출·비용추출·점검 네 곳에서
보고서명에 「정정」이 들어가면 건너뛴다. 원본은 이미 원장에 따로 있고, 실적추출은
예전부터 「기재정정」을 빼고 있었으므로 방침도 일관된다(「첨부정정」이 새어 들어와
현대글로비스가 중복 추출된 적이 있다).

**최종 상태**: 저장안됨 0 · 실적남음 0 · 비용남음 0. 실적 줄 1,777 → **1,868**,
비용 줄 44,581 → **44,978**. 스물여섯 회사가 모두 값을 갖췄다.

**파서 판을 올릴 때 고칠 세 곳** — 값이 어긋나면 조용히 안 맞으므로 함께 본다:
`실적추출 「부문 거르기」`, `실적추출 「읽을 보고서 고르기」`, `수집상태 점검
「대조 요약」`. 지금은 모두 `v3`.

**수동 실행은 20건이 한계다.** 셋째 판을 이어 돌렸더니 `crashed` 로 죽었다 —
n8n 수동 실행은 아이템을 전부 메모리에 물고 있어서 큰 원문 여러 건이 겹치면
넘친다. 예약 실행에서는 나지 않던 문제다. 밀린 것을 손으로 몰아 처리할 때는
한 판 끝날 때까지 기다렸다가 다음 판을 돌린다.

**주의**: 판토스는 이제 감사보고서(2022~2024)와 사업보고서(2025)가 원장에 함께
있다. 같은 사업연도가 두 보고서에서 나오면 `경쟁사실적`에 줄이 둘 생긴다 —
중복 방지가 접수번호 단위이기 때문이다. 지금은 겹치는 해가 없지만, 2026년
사업보고서가 2024·2025 를 다시 실으면 겹친다. 그때 조회 웹훅의 묶기 규칙을 봐야 한다.

### 대시보드 구성을 다시 짰다 (2026-09-08)

메뉴 이름과 화면 구성을 사용자 요청대로 바꿨다. 모두 `hub.html` 안의 일이고
n8n 쪽은 손대지 않았다.

**① 이름** — 종합 상황판 → **Home**, 전체 기사 조회 → **키워드 뉴스**,
자본시장 딜 → **자본시장 딜 현황**. 나머지 둘은 그대로다.

**② Home** — 공통 지표를 유사한 것끼리 묶고(`HOME_IND`: 환율 / 기준금리 /
국채 3년 / 유가) 주요 지표만 남겼다. 자본시장 딜 카드는 최근 7일 등록분만 센다.

**③ 계열사 현황** — 하위 탭 줄(`#coSub`)을 없애고 **오른쪽 세로 계열사 줄**
(`#coTabs`, `.co-rail`)로 바꿨다. 「그룹 전체」를 고르면 그룹 실적만, 계열사를
고르면 경쟁사 성과와 리스크 관리가 함께 선다. 시장 지표는 **왼쪽 메뉴의 별도
항목**(`#tab-index`)으로 나갔고 계열사 줄(`#indTabs`)을 따로 그리되 `coTab`을
공유한다. 실적 보기는 매출액·영업이익 둘만(믹스·이익률 삭제), 기간은 연도별·
분기별 둘만 남겼다. 누계로 신고하는 회사(`분기꼴 === '누계'`)는 `comp분기로()`가
같은 해 앞 분기를 빼서 진짜 분기값으로 만든다.

`CFG_판 = 3`으로 올려 옛 설정의 `하위순서`·`sub` 키를 지운다.

**④ 자본시장 딜 현황 — 하위 탭 둘** — 「📄 주요 공시」(DART)와
「📰 관련 보도」(뉴스)로 나눴다. 공시는 정확한 대신 늦고(이사회를 지난 뒤에야
올라온다) 뉴스는 빠른 대신 「검토 중이라고 한다」까지라, 둘을 한 화면에 둬야
딜의 앞뒤가 맞는다.

- 관련 보도는 **이미 받아 둔 `allArticles`**에서 그룹 이름에 「자본시장」이 든
  것만 뽑는다. 웹훅을 새로 부르지 않는다.
- 그룹 이름을 박아 두지 않고 받아온 기사에서 찾아 버튼을 만든다. 기본값은 딜
  단계를 말하는 **「자본시장_딜」** 그룹이다 — 「자본시장」 그룹은 시장 논평이
  대부분이라 함께 깔면 딜 기사가 묻힌다.
- **날짜 범위를 두지 않는다.** 뉴스 수집은 주 1회라 공시처럼 이레로 자르면
  수집 직후가 아니면 빈 화면이 된다. 대신 머리글에 실제 수집 기간과 건수를 적는다.
- 켜 둔 하위 탭은 `lx-deal-pick`으로 기억한다.

**⑤ 그래프 손질 (같은 날 이어서)** — 지표·경쟁사 선 그래프를 네 가지 고쳤다.

- **위쪽 잘림**: 그림판 여백을 위아래 같게(14/14) 두었더니 꼭짓점이 왼쪽 위
  최댓값 눈금 글자(`.ind-y.hi`)에 덮이거나 맨 위 선에 붙어 잘린 것처럼 보였다.
  위 26 / 아래 14 로 바꿨다(막대 그래프는 위 20 / 아래 8). 높이 비율 셈은
  `ind높이비율()` 하나로 모았다 — 그림·커서 점·기준선 글자가 각각 제 셈을 하면
  조금씩 어긋나고, 어긋난 뒤에는 어느 쪽이 맞는지 알 수 없다.
- **곡선**: `polyline` 을 베지에 `path`(`ind곡선()`)로 바꿨다. 조종점의 세로
  자리는 **양 끝 값 사이로 잡아 둔다** — 안 그러면 곡선이 실제 값보다 위로
  솟구쳐 있지도 않은 최고점을 그린다.
- **굵기**: 1.6~2.2 → 2.2~2.8(나라 구분 그래프는 2.3).
- **커서 점**: 안내선이 각 선을 지나는 자리에 속 빈 동그라미를 찍는다
  (`.ind-dots`). **늘 찍혀 있던 마지막 값 점은 없앴다** — 마지막 값에만 점이
  박혀 있으면 그 점이 무슨 뜻인지 묻게 되고, 커서를 올렸을 때 생기는 점과
  헷갈린다. 점은 「지금 가리키는 곳」이라는 뜻 하나만 가진다.
  SVG 원이 아니라 자리를 백분율로 준 HTML 로 찍는다 — 이 그림판은
  `preserveAspectRatio="none"` 이라 SVG 안의 원이 화면에서 납작한 타원이 되고
  오른쪽 끝에 걸린 것은 절반이 잘린다. `.ind-svg{overflow:visible}` 도 같은
  까닭이다(양 끝 선 굵기의 절반).

검증은 `scratchpad/그래프시험.mjs` 로 했다 — 곡선이 값 범위를 벗어나지 않는지,
꼭대기가 눈금 글자보다 아래인지, 커서 원이 선 위 2px 안에 놓이는지를 잰다.

### 계열사 주요 공시 (2026-09-08 신설)

「주요 공시를 계열사 현황 아래로」 요청에 대한 답이다. 자본시장 딜 현황의
「주요 공시」는 시장 전체 딜이라 그대로 두고, 계열사 화면에 **그 계열사와 등록된
경쟁사가 낸 DART 공시 전부**를 새로 붙였다.

**① 새 시트 「회사공시」** — 컬럼 `접수번호 · 접수일 · 회사명 · corp_code · 계열사 ·
관련 · 시장 · 보고서명 · 구분 · 제출인 · 비고 · 수집일시`.

**② `LXGroup_회사공시수집`(`H03ChiOEQZhutaL5`)** — 금 08:45 예약 + Execute Workflow
Trigger. `경쟁사` 시트에서 `사용=TRUE` 인 회사의 **corp_code 로** `list.json` 을
회사마다 부른다(`pblntf_ty` 없이 → 모든 종류). 처음 보는 회사는 90일 네 쪽,
이미 90일치를 쌓아 둔 회사는 30일 두 쪽. 접수번호로 중복을 거른다.
`saveDataSuccessExecution: none`(주소에 DART 키가 들어간다).

- 구분 여덟: 정기보고서 · 감사보고서 · 실적·전망 · 주요사항 · 자금조달 ·
  지분·주주 · 지배구조 · 기타. **차례가 뜻을 가른다** —
  「대규모내부거래에대한이사회의결정」이 '결정'으로도 걸리므로 지배구조를
  주요사항보다 먼저 본다.
- 첫 적재에서 두 번 고쳤다: 「최대주주등소유주식변동신고서」가 기타로 떨어져
  지분·주주에 `최대주주|소유주식변동` 을 넣었고, 자금조달의 '출자'가
  「타법인주식및출자증권처분결정」을 잡아가서 '출자'를 뺐다(그건 딜이다).
  분류를 고칠 때는 「회사공시」 시트를 머리줄만 남기고 비운 뒤 한 번 돌린다.

**③ `LXGroup_회사공시조회`(`8dqSPosl4vVDYKUQ`)** — 웹훅 `lxgroup-회사공시`.
`{ code, 계열사 }` 를 받아 그 계열사 것만 최신순 600건까지 돌려주고 구분별·회사별
건수를 함께 준다. 계열사로 나누는 까닭은 스물여섯 곳 석 달치를 통째로 보내면
화면 여는 데만 한참 걸리기 때문이다.

**④ 화면** — `#co-disc` 카드를 경쟁사 성과와 리스크 관리 사이에 넣었다. 받아 둔
것은 `discData[계열사]` 로 계열사별로 기억한다. 구분 버튼(건수 포함, 0건은 안 세움)·
회사 선택·보고서명 검색·「계열사가 낸 것만」·30건씩 더보기.

**⑤ 「업데이트하기」** — `LXGroup_수동실행` 의 경쟁사 갈래(공시수집 → 원문저장 →
실적추출) 끝에 회사공시수집을 이었다.

2026-09-08 첫 적재: 인터 64 · 판토스 45 · 하우시스 72 · 글라스 2 · 세미콘 57 ·
MMA 71 · 홀딩스 0 = **311건**. 홀딩스가 0인 것은 `경쟁사` 시트에서 LX홀딩스 줄의
`사용`이 FALSE 이기 때문이다(전부터 보류 중인 항목).

검증은 `scratchpad/화면시험.mjs`(Playwright)로 했다 — 웹훅을 전부 가로채
가짜 데이터로 띄우고 40가지를 확인한다. 기사 픽스처의 칸 이름은 조회 웹훅이
주는 그대로 `articles` / `group` / `title` / `collectedAt` 이어야 한다. 한 번
`딜` 픽스처에서 날짜 칸을 `접수일`로 잘못 적어, 화면 코드의 같은 실수를 시험이
통과시킨 적이 있다. 실제 칸 이름은 `번호` · `회사` · `일`이다.

---

## 3. 구글 드라이브 구조

```
LX Group MI/
├─ LX_Group_MI_Setting            (설정 스프레드시트)
│   탭: 설정 · 경쟁사 · 경쟁사실적 · 경쟁사비용 · 시장지표 · 시장지표값
│       · 경쟁사공시 · 소송사건 · 리스크 · 딜공시(신규)
├─ 경쟁사 분석/
│   ├─ 경쟁사 부문매핑
│   └─ 홀딩스·인터·하우시스·글라스·판토스·세미콘·MMA/
│        └─ <회사명>/  ← DART 원문 + 「<회사명> 실적 마스터」 (23개)
├─ 시장 지표/                      폴더 ID `1hSI7BuESsF31DfxlDtvZUx9ZSFLsZ_4V`
│   └─ 공통·인터·판토스·하우시스·글라스·세미콘·MMA 지표 마스터 (7개)
│      탭: 지표 목록 · 월별 · 일별 · 연도별
└─ 리스크 관리/                    폴더 ID `1Yxp7Fr1keGnXdne40QiBTd7Q2WThciEV`
    └─ 인터·판토스·하우시스·글라스·세미콘·MMA·홀딩스 (빈 폴더 7개)
```

---

## 4. 리스크 관리 구조

`LX_Group_MI_Setting`의 **`리스크` 시트** 한 줄이 곧 대시보드의 리스크 하나다.
코드 수정 없이 시트만으로 추가·수정할 수 있다.

컬럼: `계열사 · 리스크명 · 사용 · 키워드 · 설명 · 사건원장 · 비고`

- **키워드**: 쉼표 구분. 기사의 제목/매칭어/그룹명/요약에 대해 부분 일치로 검사한다.
  **그룹명을 넣으면 그 그룹 기사 전체가 포함된다.**
- **사건원장**: `TRUE`면 「주요 소송 사건 · 책임 배분」 카드가 함께 표시된다.
  현재 실리코시스만 `TRUE`.
- 한 계열사에 여러 줄이 있으면 화면 위쪽에 선택 버튼이 생긴다.

**현재 등록: 1줄 (하우시스 / 미국 엔지니어드스톤 리스크 / 키워드 24개 / 308건 매칭)**

나머지 6개 계열사는 빈 화면 상태이며, 화면에서 시트와 드라이브 폴더로 바로 이동할 수
있는 링크를 제공한다.

---

## 5. 다음 할 일

### A. 계열사별 리스크 등록 (보류 중 — 사용자 결정 대기)

사용자가 테마를 지정했고, 키워드 초안까지 만들었으나 **아무것도 반영하지 않은 상태**다.

| 계열사 | 테마 |
|---|---|
| 인터 | 인도네시아 정세 (자원·정책 리스크) |
| 판토스 | 이란 전쟁 등 해상 경로 이슈와 운임 변동 |
| 글라스 | 중국·인도산 반덤핑 |
| MMA | 중국산 수입량·증설 |
| 세미콘 | 디스플레이 시장 증감 (DDI 수요) |
| 하우시스 | 실리코시스 (현행 유지) |

**핵심 제약**: 리스크 탭은 수집 그룹이 있어야 동작한다. 실리코시스가 308건인 이유는
`해외_주요이슈` 전용 수집 그룹이 있기 때문이다. 위 5개 테마를 현재 코퍼스(2,506건)에
대고 측정하면 1~4건이고 대부분 오탐이다.

따라서 리스크 하나당 두 가지가 필요하다:
1. 신청/변경 화면에 **수집 그룹** 추가 (키워드 문법: `+` 필수포함, `-` 제외)
2. `리스크` 시트에 **한 줄** 추가 (그룹명 + 고유 단어 몇 개)

**미결 사항**: 수집 그룹을 만들면 그 기사가 팀 메일·텔레그램에도 발송된다.
현재 하루 139건 수집 → 98건이 기준 통과해 메일에 실린다. 선택지는
(a) AI 평가 켬 = 점수가 붙어 리스크 화면이 온전히 동작, 메일도 늘어남
(b) AI 평가 끔 = 메일 그대로, 리스크 화면에 중요도·별점 없음
(c) 인터·판토스만 먼저 1주일 시험 ← 권장
사용자가 "일단 아무것도 반영하지 마"라고 지시해 대기 중이다.

키워드 초안은 세션 대화에 있으며, 설계 원칙은 다음과 같다:
- 짧은 토막 금지. `DDI`는 `BuildDirect` 안에 부분 일치해 걸린다. `MMA`는 격투기·
  공시를 물어온다. `증설` 단독은 데이터센터 기사를 대량으로 끌어온다.
- 두 단어 이상의 고유한 표현을 쓴다(`산림 훼손`, `해상운임`, `상계관세`).
- 모호한 기준어는 `+`로 묶는다(`인도네시아+니켈`). `인도네시아` 단독은 판토스
  인도네시아 CSR 기사 7건이 걸렸다.

### B. 그룹 실적 — LX판토스·LX글라스 실적 추출 (2026-09-07 해결)

**조사 결과**: 일곱 계열사 모두 `경쟁사` 시트에 `우리편=TRUE`로 이미 등록돼 있었다.
화면에 안 뜬 까닭은 등록이 아니라 다음 둘이었다.

1. **조회 웹훅이 「전사」 줄을 버렸다 (고침)** — 부문표가 없는 회사는 추출기가 요약
   손익의 전사 값을 부문 이름 없이(또는 `—`) 남기는데, `LXGroup_경쟁사_조회`의
   `답 만들기` 부문 필터가 그것을 쓰레기로 보고 버렸다. 뒤에 `전사 보태기` 노드를
   붙여, 부문이 하나도 안 잡힌 회사만 「전사」 한 줄로 되살린다. 이것으로
   **LX세미콘·LX MMA**가 살아났고 경쟁사 쪽에서도 8곳(현대글로비스·SK네트웍스·
   아나패스·금호석유화학·대한유화·이건창호 등)이 함께 살아났다.

2. **LX판토스·LX글라스는 숫자 자체가 없었다 (고침)** — 진단해 보니 원인은 하나였다.
   두 회사의 요약 손익표는 과목을 **「I. 매출액」·「V. 영업이익」**으로 적는데,
   `부문 실적 뽑기`의 이름 읽기가 이름이 딱 「매출액」일 때만 알아본다. 그래서
   요약 손익 되돌림이 그 표를 통째로 지나쳤다. 연도(`제 48(당)기`)와 원 단위는
   기존 코드가 이미 다룰 줄 알았다 — 표를 못 읽으니 거기까지 가지 못했을 뿐이다.

   **고친 방법**: `부문 실적 뽑기`는 건드리지 않고, 뒤에 `요약 손익 되줍기` 노드를
   새로 붙였다. 앞 노드가 「부문표 못 찾음」 표시줄만 낸 보고서에서만 돌고, 그때
   과목 앞머리 번호(`I.` `V.` `1)`)를 떼고 다시 읽는다. 이름 읽기를 앞 노드에서
   느슨하게 바꾸지 않은 까닭은 그 함수가 **부문표 판정에도 쓰여서**, 느슨해지면
   이미 숫자가 잘 나오는 스무 회사에서 엉뚱한 표가 부문표로 잡힐 수 있기 때문이다.
   못 주우면 받은 표시줄을 그대로 흘려보낸다.

   후보표를 고를 때 **「종속기업」은 제외어에 넣지 않았다** — 연결재무제표 머리글이
   늘 「…와 그 종속기업」이라, 넣으면 정작 봐야 할 연결손익계산서가 통째로 빠진다
   (실제로 처음에 그 실수로 판토스가 안 나왔다).

   **결과**(라이브 확인): LX판토스 2021 7.82조 / 2022 10.67조 / 2023 6.88조 /
   2024 8.06조, 영업이익 3,604억 / 3,745억 / 1,560억 / 2,219억.
   LX글라스 2024 3,838억(영업이익 24억) / 2025 3,146억(영업이익 -345억).
   케이씨씨글라스처럼 부문표가 있는 회사는 되줍기가 열리지도 않는다 — 부문 이름이
   열 머리글이라 기(期)를 못 찾고 조용히 지나간다(그대로가 맞다).

   **되읽히게 만든 방법**: 추출기는 「경쟁사실적」에 이미 있는 접수번호를 다시 읽지
   않는다. 그래서 두 회사의 표시줄 10개의 `접수번호`를 비우고 `단위`에
   `되읽기 대기 (원래 접수번호)`를 남겼다(`TMP_판토스글라스_되읽기표시`). 줄을 지우지
   않은 까닭은 지우면 아래 줄 번호가 밀려 다음 지우기가 엉뚱한 줄을 건드리기 때문이다.
   남은 표시줄은 부문이 `—`라 `답 만들기`가 알아서 버린다.

`LX홀딩스`는 시트에 `사용=FALSE`다(줄 2, 메모: "지주사 연결. 필요해지면 사용을
TRUE 로"). 켜면 그룹 실적에 지주사 연결 실적이 들어오는데, 자회사 매출을 다시 한 번
세는 셈이라 나란히 놓으면 오해를 부른다. **이것도 사용자 판단이 필요하다.**

### C. 「실적 마스터」 중복 파일 재확인

`이미 있나` IF 노드가 드라이브 응답에 `files` 키가 아예 없는 경우까지 "없음"으로
판정해 파일을 계속 새로 만들던 버그를 수정하고, 쌓인 20개를 휴지통으로 옮겼다.
**하루 이틀 뒤 파일 수를 다시 세어 확인 필요**(현재 23개, 폴더당 1개).

### D. 문체 정리 (완료)

`README.md`·`hub.html`·`manual.html`·`docs/`의 부자연스러운 순우리말 어휘를 표준
용어로 모두 바꿨다(2026-09-06). `hub.html`에서는 주석뿐 아니라 식별자도 함께 바꿨다:

| 옛 이름 | 새 이름 |
|---|---|
| `고르개` (함수) | `설정선택` |
| `고르개` (지역 변수) | `선택버튼` |
| `돈고르개` / `모양고르개` / `기간고르개` | `통화선택` / `모양선택` / `기간선택` |
| `단추` / `옛단추` | `버튼` / `옛부문버튼` |
| `말썽` | `오류메시지` |
| `리스크낱말` / `집낱말` / `낱말` | `리스크키워드` / `집키워드` / `키워드` |
| `갈래` | `구분` |

`encodeURIComponent('갈래')` 문자열 하나만 남겨 뒀다 — `LXGroup_원본내려받기` 웹훅이
받는 질의 파라미터 이름이라 바꾸면 다운로드가 깨진다.

`눈금`(축 눈금), `띠`(막대 옆 표시 띠)는 표준 한국어라 그대로 뒀다.

검증: `찍기.mjs`(가로 넘침·JS 에러 없음), `계열사.mjs`(13개 항목 통과),
`설정.mjs`(18개 항목 통과).

---

## 6. 검증 도구

Playwright 기반 스크립트가 세션 스크래치패드에 있다(세션 종료 시 사라짐).
필요하면 다시 만든다.

| 스크립트 | 역할 |
|---|---|
| `찍기.mjs` | 탭별 스크린샷 + 가로 넘침 + JS 에러 검사 |
| `재기2.mjs` | 탭별 페이지 높이 측정 |
| `좁게.mjs` | 320/360/390/430px 가로 넘침 검사 |
| `계열사.mjs` | 계열사 구조 동작 검증 |
| `설정.mjs` | 주요 뉴스·리스크 탭·대시보드 설정 검증 |
| `리스크등록.mjs` | 시트 줄 추가 시 리스크 표시 검증 |
| `딜.mjs` | 자본시장 딜 탭·카드 검증(구분 필터·검색·더보기·가로 넘침) |
| `화면시험.mjs` | 2026-09-08 개편분 40가지 검증(메뉴 이름·Home 주요 지표·계열사 줄·분기 차분·딜 7일·딜 하위 탭 둘) |
| `그래프시험.mjs` | 선 그래프 검증(곡선·굵기·최상단 잘림·커서 원 위치) |

웹훅 응답은 `lookup7.json`(경쟁사), `ind.json`(지표), `art.json`(기사), `딜.json`(딜공시)으로 저장해
`page.route`로 주입한다. 브라우저에서 외부 네트워크는 사용할 수 없다.

### `LXGroup_수집상태_점검` (`HKRcmGvzMcJ0kyCu`)

수집이 어디까지 됐는지 한 표로 보는 n8n 워크플로우다. **손으로만 돌린다**
(수동 트리거뿐이라 예약 실행이 없다). `경쟁사공시` 원장과 `경쟁사실적` 시트를
회사별로 대조해 이렇게 낸다:

| 칸 | 뜻 |
|---|---|
| `원장건수` | 원장에 있는 공시 수 |
| `저장안됨` | `처리`가 「저장」이 아닌 것 — 원문 내려받기가 걸린 건 (정정공시는 안 센다) |
| `실적남음` | 저장은 됐는데 아직 실적추출이 안 읽은 보고서 수 |
| `비용남음` | 같은 것을 비용추출 기준으로 센 수 (감사보고서는 안 세고, 판 재시도 규칙도 없다) |
| `실적줄` / `표시줄` | 실적 시트의 줄 수 / 그중 「부문표 못 찾음」 표시줄 |
| `실적연도` | 실제로 값이 있는 사업연도 목록 |

`실적남음 + 비용남음`이 큰 순으로 정렬한다. 첫 줄은 합계다. 시트를 고치지 않으므로
아무 때나 돌려도 된다. 표시줄 판정은 실적추출의 `판` 상수(지금 `v2`)와 같은 값을
쓴다 — 파서 판을 올리면 여기도 같이 올린다.

**밀린 것을 다 채우는 데 드는 실행 수** — 대기 건수를 배치 크기로 나누고, 마무리로
부문매핑마스터와 마스터정리를 한 번씩 더한다.

| 단계 | 한 번에 |
|---|---:|
| 원문저장 | 25 |
| 실적추출 | 20 |
| 비용추출 | 12 |
| 부문매핑마스터 · 마스터정리 | 전체 다시 쓰기라 각 1회 |

2026-09-08 에는 원문저장 2 · 실적추출 24 · 비용추출 5 로 여섯 번 계산이 나왔고
실제로 여섯 번에 끝났다. 그 뒤 파서를 v3로 올리면서 지난 실패분이 되살아나
실적추출 2회 · 비용추출 1회 · 마무리 2회를 더 돌렸다.

금요일 정기 실행이 이 워크플로우들을 각각 1회씩 돌리므로 **손대지 않아도 보통
2주면 채워진다.** 손으로 당길 때는 **`production` 모드로 돌린다** — `manual` 은
아이템을 전부 메모리에 물고 있어 큰 원문이 겹치면 `crashed` 로 죽는다.

대시보드 「↻ 업데이트」로 당기면 버튼 한 번이 `수동실행 + 공시수집 + 원문저장 +
실적추출` 4회다. 이 경로에는 비용추출·부문매핑·마스터정리가 없어 그 셋은 금요일을
기다린다.

---

## Apps Script 이관 기반 (2026-09-07)

n8n의 회사 워크플로(시간표 10개, 조회 웹훅 4개)를 Google Apps Script로 옮기기로
했다. 뉴스 파이프라인(LXGroup_MI, 신청접수, 콜백처리)은 n8n에 남는다. 자동으로
수집하지 않고 hub.html의 「업데이트」 버튼으로 한꺼번에 수집하는 구조다.
설계는 `docs/앱스스크립트_이관_설계_2026-09-07.md`, 코드는 `apps-script/lxgroup`,
사람이 할 일은 `apps-script/README.md`다. **아직 아무것도 연결하지 않았다.**
