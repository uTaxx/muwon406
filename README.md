# LX Group MI

LX홀딩스 경영전략팀용 뉴스·공시 모니터링 대시보드입니다.
수집과 발송은 n8n(`sondullab.app.n8n.cloud`)이 하고, 이 저장소는 **화면만** 담습니다.

## 주소

<https://utaxx.github.io/muwon406/lxgroup-mi/hub.html>

## 구성

| 파일 | 역할 |
|---|---|
| `public/lxgroup-mi/hub.html` | 본체. 종합 상황판 / 전체 기사 조회 / 특정 키워드 검색 / 실리코시스 리스크 / 시장지표 / 경쟁사 성과 6개 화면과, 머리말에 놓인 설정 두 가지(키워드 신청·변경, AI 학습 데이터) |
| `public/lxgroup-mi/manual.html` | 사용설명서 |
| `public/lxgroup-mi/index.html`<br>`public/lxgroup-mi/articles.html` | 통합 전 옛 주소로 들어와도 열리도록 hub.html로 넘겨주는 스텁 |
| `public/index.html` | 사이트 첫 화면 → hub.html |
| `docs/silicosis-case-routine.md` | 실리코시스 사건 조사 루틴 절차 |

## 배포

`main`에 푸시하면 `.github/workflows/deploy.yml`이 `public/`을 그대로 GitHub Pages에 올립니다.
빌드 단계가 없습니다 — 의존성 없는 정적 HTML이라 빌드할 것이 없고, 단계를 두면
그 단계가 깨질 때 대시보드까지 같이 멈추기 때문입니다.

## 화면이 부르는 것

hub.html은 n8n 웹훅을 직접 호출합니다(`lxgroup-articles`, `lxgroup-intake`,
`lxgroup-lookup`, `lxgroup-instant`, `lxgroup-adhoc-search`, `lxgroup-feedback`,
`lxgroup-cases`, `lxgroup-case-status`, `lxgroup-knowledge`,
`lxgroup-delete-articles`).

**이 저장소는 공개입니다.** 비밀값은 두지 않습니다 — 기사 삭제에 쓰는 관리자
비밀번호는 설정 시트에 있고 확인은 웹훅(서버)에서 합니다.

## 운영 — 실패했을 때

수집·발송 워크플로가 실패하면 **`LXGroup_실패알림`**(n8n)이 관리자 텔레그램으로
바로 알립니다. 어느 워크플로가 어느 노드에서 왜 멈췄는지와 실행 링크를 보냅니다.
`LXGroup_MI` · `LXGroup_지표수집` · `LXGroup_신청접수` · `LXGroup_콜백처리`의
'오류 워크플로'로 걸려 있습니다.

`LXGroup_MI_Sub`에는 일부러 걸지 않았습니다. 하위에서 죽어도 그 노드 이름이
마스터 오류에 실려 올라오므로, 양쪽에 걸면 같은 실패로 알림이 두 번 옵니다.

외부를 부르는 노드에는 재시도가 걸려 있습니다(조회·RSS·네이버·DART·Claude 호출·
시트 저장). **이메일과 텔레그램 발송에는 일부러 걸지 않았습니다** — 재시도하다
중복 발송되면 같은 다이제스트가 수신자 전원에게 두 번 갑니다. 늦게 오는 것보다
나쁩니다.

### 발송 시각을 바꿀 때

시각은 **설정 시트의 `발송시각`이 근거**입니다. 그런데 `LXGroup_MI`의 스케줄
트리거에도 같은 시각이 적혀 있어, **두 곳을 같이 고쳐야 합니다.** 한쪽만 고치면
트리거가 안 깨우거나(수집 자체가 없음) 깨워도 그냥 지나갑니다.

2026-09-03에 이걸로 11시 수집이 하루 빠졌습니다. 실행 기록만 보고 "실제로 도는
건 07시·16시뿐"이라고 판단해 트리거에서 11시를 뺐는데, 시트에는 처음부터
`07:00, 11:00, 16:00` 세 번이었습니다. **실행 기록은 그 시각에 무슨 일이
있었는지만 말해 줍니다. 무슨 시각이어야 하는지는 시트가 말합니다.**

### 하위 워크플로를 부르고 응답을 돌려줄 때

**n8n은 어떤 노드가 0건을 내보내면 그 뒤 노드를 통째로 건너뜁니다.** 웹훅
응답 노드가 그 뒤에 있으면 **아무 답도 안 보내고 실행은 `성공`으로 끝납니다.**
화면에는 `Failed to fetch`만 뜨는데, 이건 HTTP 오류가 아니라 답이 아예 안 온
것입니다.

`MI_Sub 즉시실행` / `애드혼실행`이 이 경우였습니다. Execute Workflow 노드는
**하위 워크플로에서 마지막에 실행된 노드의 출력**을 그대로 돌려주는데, 그게
어느 노드인지는 그날 어떤 가지를 탔느냐에 따라 달라집니다. 2026-09-03에는
DART 공시가 없어 `공시행조립`이 0건으로 끝났고, 그래서 응답이 사라졌습니다.
수집·발송은 이미 다 끝난 뒤라 **텔레그램과 메일은 멀쩡히 도착합니다** — 그래서
"메시지는 왔는데 화면만 오류"라는 헷갈리는 모양이 됩니다.

두 노드에 `alwaysOutputData: true`를 켜서 빈 항목이라도 하나 내보내게 했습니다.
뒤쪽 노드들은 어차피 시트를 다시 읽어 답을 만들기 때문에 하위 워크플로의
반환값을 쓰지 않습니다. **하위 워크플로의 반환값에 기대지 않는 것**이 요점입니다.

## 화면 짜임 — 좁은 화면과 넓은 화면

이 화면은 처음부터 휴대폰 기준으로 만들어져서 본문이 **680px**로 묶여 있었습니다.
데스크톱에서는 양옆이 통째로 비는데 정작 정보는 탭 뒤에 숨어 있었고, 기능이
늘수록 "무슨 일이 있었는지"를 알려면 탭을 하나씩 눌러 봐야 했습니다.

**1024px부터** 짜임이 바뀝니다.

- 본문이 1320px까지 넓어지고, 가로 탭 줄이 **왼쪽 세로 차림표**로 서서 늘 보입니다.
- 식별코드 칸도 그 차림표 위에 같이 붙어 따라 내려옵니다(`position:sticky`).
- 상황판 카드와 시장지표 카드가 **두 칸**으로 흐릅니다.

**1024px 아래에서는 아무것도 바뀌지 않습니다.** 넓은 화면 규칙은 전부
`@media (min-width:1024px)` 안에만 있습니다. 탭이 다섯이 되면서 360px에서
글자가 잘리던 것만 `max-width:560px`에서 글자 크기를 줄여 맞췄습니다.

차림표 단추의 `<br>` 뒤에는 **일부러 빈칸을 넣어 두었습니다**. 넓은 화면에서
`br`을 숨기면 그 빈칸이 낱말 사이를 벌려 "전체 기사 조회"로 읽힙니다. 빈칸이
없으면 "전체 기사조회"가 됩니다.

## 종합 상황판

첫 화면입니다(`?tab=home`, 아무 것도 안 붙이면 여기가 열립니다).

- **KPI 넉 장** — 마지막 수집 / 최근 7일 / 중요도 4↑ 7일 / 실리코시스 30일.
  "오늘 몇 건"으로 세지 않습니다. 아침 수집이 돌기 전에 열면 0이 뜨는데,
  0은 고장으로 읽힙니다. 그래서 **마지막으로 수집이 돌아간 날**을 찾아
  그 날짜와 함께 보여줍니다.
- **최근 기사 8건**, **그룹별 수집 현황**(최근 7일, 없으면 전체),
  **실리코시스 요약**, **시장지표 공통 8개**.
- 각 카드의 "자세히 →"가 해당 화면으로 넘깁니다.

**여기서 새로 부르는 웹훅은 시장지표 하나뿐입니다.** 기사도 실리코시스도 이미
받아 둔 `allArticles`로 만듭니다 — 첫 화면이 웹훅 넷을 기다리게 되면 결국
느려서 아무도 안 봅니다.

실리코시스 머리기사는 **반드시 최근 30일 안에서** 고릅니다. 전체에서 고르면
몇 해 전 기사가 "지금 볼 것"으로 올라옵니다. 리스크 탭에서 한 번 겪은 함정이라
같은 규칙을 그대로 씁니다.

## 경쟁사 성과

계열사마다 경쟁사를 나란히 놓고 부문별 매출·영업이익을 견주는 화면입니다.
숫자는 DART 공시에서 옵니다.

### 어디서 오는가

| 회사 | 근거 | 얻는 것 |
|---|---|---|
| 상장사 | 사업보고서 「사업의 내용」·「영업부문 정보」 | 부문별 매출·영업이익, 수출/내수 |
| 비상장 외감법인 | 감사보고서·연결감사보고서 | 회사 전체. 연결이면 영업부문 주석도 |

**재무 API(`fnltt*`)는 쓰지 않습니다.** 그것은 정기보고서를 내는 회사만
나오는데, LX판토스·LX글라스·LX MMA 는 비상장이라 정기보고서가 없습니다.
셋 다 감사보고서는 내므로 원문을 읽는 쪽이 더 많이 얻습니다.

### 공시가 갈라 주는 만큼만 갈린다

하우시스 사업보고서를 열어 확인한 결과, 공시 부문은 **건축자재 / 자동차소재·
산업용필름 둘뿐**입니다. 매출실적 표의 「품목」 칸은 "창호,바닥재,솔리드서피스 등"
이라는 나열일 뿐 품목별 금액이 없습니다. 그래서 **창호·바닥재 수준의 제품군
비교는 공시로 만들 수 없습니다.**

품목 단위 흐름은 시장지표 탭이 관세청 HS 코드별 수출입으로 대신 봅니다
(판유리에 이미 쓰고 있는 방식). 두 화면의 분업이 이렇습니다.

| | 맡는 것 |
|---|---|
| 경쟁사 성과 | 회사·부문 단위 매출·영업이익 |
| 시장지표 | 품목 단위 흐름 (무역통계) |

### 숫자가 쌓이는 길

세 워크플로가 이어 달립니다. 하나로 묶지 않은 이유는 무게가 다르기 때문입니다 —
공시 목록은 가볍고, 원문은 한 건이 8~9MB입니다.

| 워크플로 | 언제 | 하는 일 |
|---|---|---|
| `LXGroup_경쟁사_공시수집` | 매일 07시 | 회사별 정기·감사보고서 목록을 `경쟁사공시` 원장에 쌓는다. 원문은 안 받는다 |
| `LXGroup_경쟁사_원문저장` | 10분마다 20건 | 원장에서 아직 안 받은 것을 골라 DART 원문을 드라이브에 저장 |
| `LXGroup_경쟁사_실적추출` | 10분마다 4건 | 저장된 원문에서 부문별 매출·영업이익을 뽑고, 「부문 거르기」로 부문 아닌 것을 버린 뒤 `경쟁사실적` 시트에 쌓는다 |
| `LXGroup_경쟁사_비용추출` | 10분마다 3건 | 같은 원문의 주석에서 전사 손익과 성격별·판관비 비용을 뽑아 `경쟁사비용` 시트에 쌓는다 |
| `LXGroup_경쟁사_마스터정리` | 30분마다 | 두 시트를 회사별로 모아 드라이브의 「실적 마스터」 스프레드시트 네 탭을 다시 쓴다 |
| `LXGroup_경쟁사_부문매핑마스터` | 6시간마다 | 공시에 새로 나타난 부문을 「경쟁사 부문매핑」 스프레드시트에 줄로 보탠다. 사람이 채운 줄은 안 건드린다 |

**어디까지 했나를 원장에 적지 않습니다.** `경쟁사실적` 시트의 접수번호로 압니다.
원장에 칸을 하나 더 만들면 같은 칸을 두 워크플로가 건드리게 되고, 그때부터 누가
먼저 썼는지에 따라 결과가 갈립니다. 숫자가 하나도 안 나온 보고서도 표시 줄
하나(부문 `—`)를 남깁니다 — 안 남기면 매번 다시 골라 읽어서 영원히 같은 파일만
받습니다.

### 원문에서 숫자를 뽑을 때 걸리는 것들

실제 공시에 대고 돌려 보며 하나씩 걸러낸 것들입니다. 다시 손볼 때 참고가 되라고
적어 둡니다.

| 걸린 것 | 어떻게 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| 기(期)가 표 안에 없다 | 제68·67·66기가 전부 '당기'로 뭉쳐 세 해가 한 해가 됨 | 표 **위 글**에서 읽는다 (`(1) 제68기 (단위 : 백만원)`) |
| 세 겹 머리글 | `제50기 / 매출액 / 금액·비율(%)` 에서 비율 27.3을 금액으로 읽어 3조 회사가 0.3억이 됨 | 아래 겹이 금액/비율이면 **금액 칸만** |
| 부문표가 아닌 표 | 종속회사 목록(태양합성㈜), 요약재무, 구성비 표가 부문인 척 들어옴 | 앞글에 '부문'이 있어야 하고, 종속기업·연결대상이면 버림 |
| 기간이 부문 이름 자리에 | `제26(당)기`, `73기(2025년)` 가 부문이 되어 전사 매출이 가짜 부문으로 붙음 | 숫자+기 또는 네 자리 연도가 든 이름은 기간으로 본다 |
| 감사보고서에 본문이 없다 | 첨부 하나(`_00761`)뿐이라 파일을 못 찾음 | 이름 규칙에 맞는 게 없으면 **가장 큰 파일** |
| 총매출 vs 외부매출 | 총매출액은 부문끼리 오간 돈이 섞여 회사끼리 견주는 잣대가 못 됨 | **외부매출액**을 먼저 쓴다 |
| 단위를 모르는 표 | 원 단위 요약재무표가 섞여 매출 6,483,819,486 이 시트에 남음 | 단위를 못 찾은 표는 통째로 버림 |
| 분기보고서의 누적 | 6·9개월 누적을 분기값 자리에 넣으면 3분기가 1분기의 세 배로 뜀 | 날짜 구간 길이로 가름 — 11개월↑ 연간, 2~4개월 그 분기, 사이는 버림 |
| **그 규칙이 부문표에는 안 먹었다** | 실제 자료로 재어 보니 Q1은 그 해 연간의 **0.245**, Q2는 **0.498**, Q3는 **0.748**(371개 표본). 부문표는 그냥 **연초부터의 누적**이었다 | 값은 공시 그대로 맞으니 빼서 분기를 만들어내지 않고, 화면에서 **「누적」으로 이름을 바로잡았다**(`'25 1~6월`). 전년 같은 시점 누적과 견주는 것은 그대로 옳다 |
| 분기 칸에 연간값이 새어듦 | 분기보고서의 비교 칸이 전년 연간인 경우가 있어, 현대코퍼레이션 철강은 2021·2022의 세 분기가 모두 그 해 연간과 똑같았다 | 분기 값이 **그 해 연간과 같으면** 분기 자료가 아니므로 조회에서 버린다(587줄 중 75줄) |

부문표가 아예 없는 회사(사업이 하나뿐인 세미콘·아나패스 같은 곳)는 **부문표를 못
찾았을 때만** 요약 손익에서 회사 전체 값을 집어 부문 없이 넣습니다. 화면이 빈
부문을 「전사」로 묶습니다.

### 회사마다 「실적 마스터」 스프레드시트

시트에 쌓인 숫자는 견주기에는 좋지만 한 회사를 깊이 들여다보기에는 불편합니다.
그래서 회사의 드라이브 폴더 안에 **「〈회사명〉 실적 마스터」** 스프레드시트를
하나씩 두고, `LXGroup_경쟁사_마스터정리`가 30분마다 네 탭을 **통째로 다시
씁니다**. 덧붙이지 않고 다시 쓰는 이유는, 파서를 고쳐 숫자가 바뀌면 옛 줄이
남아 있어서는 안 되기 때문입니다. 파일은 **이름으로 찾습니다** — 아이디를 어디
적어 두면 사람이 파일을 지웠을 때 영영 못 고치지만, 이름으로 찾으면 없을 때
새로 만듭니다.

| 탭 | 무엇이 들었나 |
|---|---|
| `기업실적` | 부문별 매출액·영업이익·이익률, 그리고 전사 손익(매출액·매출원가·매출총이익·판관비·영업이익)과 원가율·판관비율·이익률 |
| `비용구분` | 성격별 비용을 **제조 쪽과 판관 쪽으로 나눈** 표, 그리고 검증 줄 |
| `성격별원본` | 「비용의 성격별 분류」 주석을 **공시 원문 그대로** (단위도 원문 그대로) |
| `판관비원본` | 「판매비와관리비」 주석을 공시 원문 그대로 |

원본 두 탭은 가공값을 대조하려고 둡니다. 파서가 이름을 잘못 짝지었는지, 단위를
잘못 읽었는지는 **원문과 나란히 놓고 보아야** 알 수 있습니다 — 실제로 이 두 탭
덕분에 아래 세 가지 오류를 잡았습니다.

### 제조원가와 판관비를 가르는 셈

정기보고서를 내는 회사는 주석에 같은 비용을 **두 번** 적습니다.

- **비용의 성격별 분류** — 급여·감가상각비·운반비처럼 성격으로 나눈 것. 이 표의
  합계는 `매출원가 + 판매비와관리비` 와 같습니다
- **판매비와관리비** — 그중 판관비만 다시 성격으로 쪼갠 것

그러니 같은 성격끼리 빼면 제조 쪽이 남습니다: **`제조 = 성격별 − 판관비`**.
벽산 2025년으로 보면 인건비 624.2억 중 판관 338.5억을 빼 제조 285.8억,
감가상각비는 286.2억 중 32.7억만 판관이라 제조 비중이 89%입니다.

**어떤 회사는 아예 나눠서 줍니다.** 동화기업 주석은 `항목 | 판매비와일반관리비 |
매출원가 | 기능별 항목 합계` 꼴이라 짝지을 것 없이 그대로 씁니다. 이쪽이 짐작보다
훨씬 미더우므로 이 꼴을 먼저 알아보게 해 두었습니다.

**셈이 맞는지는 검증 줄로 확인합니다.** `비용구분` 탭 맨 아래에 두 줄이 있습니다 —
「성격별합계 − (매출원가+판관비)」와 「판관비 명세 합계 − 손익서 판관비」. 둘 다
**차이가 0이면 주석을 빠짐없이 읽은 것**입니다.

이 검증 줄이 실제로 오류를 잡아냅니다. 스무 회사를 다 채우고 나서 어긋난 곳을
세어 보니 57군데였고, 하나씩 따라가 「한 해를 여러 보고서가 담는 것」과 「비용
아닌 주석을 비용으로 읽은 것」 두 가지를 찾았습니다(아래 표). **차이가 0이 아닌
회사·연도는 그대로 믿지 말고 원본 탭과 대조하십시오** — 그러라고 검증 줄과 원본
탭을 둔 것입니다.

주의할 것이 넷 있습니다.

1. 성격별 주석에는 **재고자산의 변동**이 들어 있어, 차감으로 나온 값은 엄밀히
   매출원가가 아니라 (총비용 − 판관비)입니다
2. 이름이 회사마다 달라 **굵게 묶었습니다** — 성격별의 '종업원급여비용' 하나가
   판관비에서는 급여·퇴직급여·복리후생비로 쪼개져 있어, 셋을 합쳐 「인건비」로
   봅니다. 감가상각비·무형자산상각비·사용권자산상각비도 한 줄입니다
3. 판관비에만 있는 항목(경상개발비·하자보수비)은 성격별 주석에서 기타로
   뭉뚱그려져 **제조 쪽을 셈할 수 없어 비웁니다**. 0으로 채우면 '제조가 없다'로
   읽히는데 그건 사실이 아닙니다
4. **연결재무제표 주석**을 씁니다. 별도 숫자는 원본 탭의 둘째 묶음에 있습니다

### 주석을 읽다가 걸린 것들

| 걸린 것 | 어떻게 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| 연결과 별도를 합침 | 한 보고서에 같은 주석이 두 번(연결·별도) 나와 벽산 인건비가 624 + 397 = 1,021억이 됨 | 표마다 따로 담고 **먼저 나온 것(연결)만** 쓴다. 원본 탭에는 둘 다 남긴다 |
| XBRL 꼬리표 | 판관비 명세가 `급여, 판관비`라 성격별 `급여`와 짝이 안 맞아 제조원가가 성격별 전액으로 나옴 | `, 판관비` · `, 사용권자산` 같은 꼬리표를 떼고 짝짓는다 |
| COLSPAN으로 늘어난 칸 | 같은 기(期)가 두 열에 걸쳐 동화기업 성격별 합계가 정확히 두 배 | 한 표 안에서 같은 기는 **한 번만** 센다 |
| 주석까지 못 감 | 표를 400개까지만 훑었는데 사업보고서 표는 수백 개고 비용 주석은 29~31번 | 볼 만한 표만 세고, 태그를 벗기기 전에 값싼 정규식으로 먼저 거른다 |
| 부문 이름이 두 벌 | 사업보고서의 '건자재부문'과 감사보고서의 '건자재'가 나란히 섬 | **가장 최근 해에 회사가 쓴 이름**만 남긴다 |
| 부문 자리에 기간 | 자본변동표의 '당기말'·'전기말'이 케이씨씨 부문으로 섬. 버릴 이름 목록에 '당기'는 있어도 '당기말'은 없었다 | 뽑은 뒤 **「부문 거르기」 노드**에서 기간꼴 이름을 버린다. 화면과 마스터도 각자 막는다 |
| 비용 아닌 주석을 비용으로 | LX하우시스 판관비원본에 **기타유동자산**(유동선급금·부가세대급금)이 들어옴. 표 앞 1500자에 '판매비와관리비'라는 말이 있으면 판관비 명세로 보았는데, 바로 앞 주석이 판관비였을 뿐 | 표 **안에** 비용다운 이름(급여·감가상각비·운반비 …)이 **둘 이상** 있어야 비용 주석으로 본다 |
| 옆 주석이 딸려 들어옴 | 케이씨씨글라스는 판관비 주석 **앞에 퇴직급여 주석**이 있어, 확정급여제도 표가 판관비 명세 자리를 차지함(비용다운 이름은 둘 이상이라 위 빗장은 통과) | 표 **자신이나 바로 앞 제목(120자)**에 '판매비와관리비'가 있어야 한다. 1500자는 옆 주석까지 닿는다 |
| 한 해를 여러 보고서가 담음 | 2025년 사업보고서에 2023~2025년이 실리고 2024년 사업보고서에도 2024년이 실려, 두 벌이 섞이며 손익과 주석이 다른 보고서에서 와 검증이 어긋남(스무 회사에서 57군데) | 마스터가 **해마다 접수번호가 가장 큰 보고서 하나만** 쓴다 |
| append 가 빈 줄을 쌓음 | 부문매핑에 보탤 줄이 없어도 append 에 빈 값을 보내, 돌 때마다 표 꼬리에 빈 줄이 하나씩 늘어남 | append 를 버리고 **자리를 셈해** `values:batchUpdate` 로 쓴다. 보탤 게 없으면 아예 안 쓰고, 이미 생긴 빈 줄은 다음 줄이 덮는다 |
| 옮김이 두 단계에서 끊김 | 「건자재」→「건자재부문」은 자동으로 묶였는데 사람이 대표 줄에 「건축자재」를 적어도 「건자재」는 중간에 멈춰 화면에서 둘로 갈라짐 | 조회 웹훅이 **사슬을 끝까지 따라간다**(최대 여덟 걸음, 고리는 짝 없음) |
| 부문 아닌 것이 셈에 듦 | 화면은 「당기말」·「연결후금액」·「미국」 따위를 가려 두었지만 조회 웹훅은 그대로 세어 「짝 못 찾은 부문」이 90개로 부풀었다(129 덩어리 중 54가 부문 아님) | 부문매핑 마스터가 쓰는 **똑같은 거르개**를 조회 웹훅에도 둔다 — 둘이 같은 것을 부문으로 봐야 한다 |
| 총계 줄 이름이 「합계」가 아님 | 한진은 판관비 명세의 총계를 **「판매 및 일반관리비」**라 적는다. 항목으로 세어져 합이 꼭 두 배(1332.2억 → 2664.4억)가 됐다 | 총계 이름 목록을 넓히고, 이름으로 못 알아보면 **앞서 더한 값과 같은지**로 안다(0.5% 안, 세 항목을 더한 뒤부터) |
| 두 주석이 한 제목 아래 | LX인터내셔널은 성격별 주석과 판관비 주석을 나란히 두고 성격별 합계를 「매출원가 및 **판매관리비** 합계」라 적는다. 앞 표까지 판관비로 읽혀 두 표가 하나로 합쳐지고 판관비가 15.1조 부풀었다 | **매출원가다운 줄**(`…매출원가`·재고자산의 변동·원재료 사용액)이 있으면 판관비 명세가 아니라 성격별 표로 본다 |
| 남길 줄만 다시 쓰기 | 회사 하나를 시트에서 걷어낼 때 A2부터 남길 줄만 썼더니, 예전에 더 길었던 만큼의 **꼬리가 그대로 남아** 지운 210줄이 뒤에 되살아났다 | 쓰기 **앞에서 값 영역을 통째로 비운다** |
| 총계 자물쇠가 항목을 잡아먹음 | 값으로 총계를 알아보는 자물쇠를 아무 줄에나 0.5% 틈으로 걸었더니, 한진 성격별의 **「운임 등」(771,796)이 바로 앞 누계 769,955 와 0.24% 차이**라 총계로 오인돼 사라졌다. 7,718억이 비었다 | 자물쇠를 **표의 맨 아래 값 줄에만** 걸고 틈도 0.2%로 좁힌다. 총계는 어차피 맨 아래 있다 |
| 재무상태표를 비용으로 | 케이씨씨글라스 감사보고서의 **재무상태표**가 판관비 명세로 읽혀 5.9조가 들어왔다. 「재고자산」과 「기타장기종업원급여부채」가 비용다운 이름 둘을 채워 앞 빗장을 지났다 | `자산총계`·`부채총계`·`자본금`·`유동자산` 같은 **재무상태표 표지**를 만나면 아예 뛰어넘는다 |

### 부문매핑

회사마다 부문 이름이 다릅니다 — 하우시스는 「건축자재」, KCC는 「도료/건자재/
실리콘」, 한샘은 「홈리모델링/홈퍼니싱」. 우리 부문과 상대 부문을 짝지어 두는
표가 「경쟁사 분석」 폴더의 **「경쟁사 부문매핑」** 스프레드시트입니다. 설정 시트가
아니라 따로 둔 이유는, 이 표는 워크플로가 읽는 설정이 아니라 **사람이 손으로
채우는 곳**이기 때문입니다. 다른 실적 마스터들과 같은 폴더에 있어야 찾습니다.

`LXGroup_경쟁사_부문매핑마스터`가 여섯 시간마다 공시에 새로 나타난 부문을 줄로
보탭니다. **이미 있는 줄은 건드리지 않습니다** — 사람이 채운 짝이 지워지면
안 되니까요. 세 탭입니다.

| 탭 | 무엇이 들었나 |
|---|---|
| `부문매핑` | `계열사 · 회사명 · 상대부문 · 우리부문 · 사용 · 최근해 · 최근매출액 · 메모 · 처음본날`. 사람이 `우리부문`을 채우고 `사용`을 TRUE로 바꿉니다 |
| `우리부문` | 골라 적을 수 있는 우리 계열사 부문 목록. 뭘 적어야 하는지 몰라 헤매지 않게 |
| `안내` | 쓰는 법. 줄을 지우면 다음 차례에 빈 채로 다시 생기니, 짝이 없으면 지우지 말고 `사용`을 FALSE로 두라는 것까지 |

스스로 채워 두는 것이 둘 있습니다. **우리편 회사의 부문**은 그 자체가 기준이니
제 이름을 그대로 넣습니다. **같은 회사가 보고서마다 달리 쓴 이름**은 묶습니다 —
사업보고서의 「유리부문」과 감사보고서의 「유리」, 벽산의 「건자재」와
「건자재부문」이 그렇습니다. 꼬리(`사업부문`·`부문`·`사업`)를 떼어 속살이 같으면
한 묶음으로 보고, **가장 최근 해에 쓴 이름**을 대표로 삼습니다. 어느 사업을 우리
무엇과 견줄지는 셈이 아니라 판단이라 거기까지만 합니다.

그래서 옮김이 두 단계가 됩니다: 「건자재」→「건자재부문」→(사람이 채운)「건축자재」.
조회 웹훅이 **사슬을 끝까지 따라갑니다.** 한 번만 따라가면 「건자재」가 중간에
멈춰 화면에서 둘로 갈라집니다. 서로 가리키는 고리는 짝 없음으로 봅니다.

짝을 못 찾은 부문은 **조용히 버리지 않고** 화면 위 "짝 못 찾은 부문 N개"로
띄웁니다. 회사가 부문 이름을 바꾸면 그때 눈에 띄어야 사람이 한 줄 채울 수
있습니다.

### 화면

- 계열사 하위 탭 → 그 안에서 **회사마다 블록** 하나, 블록 안에 그 회사의 **사업부문**이
  큰 것부터. 부문마다 블록을 세웠더니 하우시스 탭 하나가 39칸이 되어 무엇을 보는지
  놓쳤다. 회사가 바깥, 부문이 안이다 (39칸 → 7칸)
- **가장 최근 기간에 자료가 없는 부문은 접어 둔다.** 회사가 보고서마다 이름을 바꾸면
  옛 이름이 그대로 남는다 — 현대리바트 13줄 중 8줄이 2021~2022에서 끊긴 이름이다.
  지우지 않고 「옛 부문 N개 더 보기」로 편다. 접은 사업인지 이름만 바뀐 것인지는
  화면이 가릴 수 없으므로 **숨기되 없애지 않는다** (현대리바트 13→5, 한진 10→4,
  벽산 9→4, 현대코퍼레이션 9→6)
- 블록 차례는 **우리 것이 먼저, 그다음 회사 덩치 순**. 덩치는 있는 부문을 다 더하지
  않고 **가장 최근 기간에 자료가 있는 부문만** 더한다 — 회사가 보고서마다 부문 이름을
  바꾸면 옛 이름과 새 이름이 나란히 남아 두 번 세어진다(현대리바트 4.1조/실제 1.5조,
  한진 5.1조/실제 3.1조)
- 블록마다 **연도별 / 분기별**을 고르고, 보기를 **매출·이익 / 매출액 / 영업이익 /
  이익률** 중에서 고릅니다. 기간과 보기는 **블록마다 따로 기억**합니다 — 건자재는
  분기로 보면서 물류는 연도로 두고 싶은 것이 자연스럽습니다
- 그 부문에 분기 자료가 아직 없으면 **「분기별」 단추가 흐려집니다**. 눌리게 해놓고
  빈 그래프를 보여주면 고장으로 읽힙니다
- **우리 계열사는 붉은 선**에 바탕색을 깔고 「우리」 표를 답니다
- 그래프·표·엑셀은 시장지표 것을 **그대로 재사용**합니다(`indPlot`·`indTableHtml`·`xlsx`)
- 회사끼리 덩치가 3배 넘게 벌어지면 **기준칸 = 100**으로 맞춰 움직인 폭만 견줍니다.
  롯데케미칼 20조와 대한유화 2조를 한 축에 그대로 얹으면 작은 쪽이 바닥에 붙은
  직선이 됩니다. 영업이익률(%)은 애초에 견줄 수 있어 그대로 둡니다
- **적자가 섞이면 지수로 안 바꿉니다.** 적자 난 해를 100 기준으로 나누면 -180 같은
  수가 나오는데 이건 '적자가 깊어졌다'가 아니라 그냥 읽을 수 없는 숫자입니다.
  그럴 때는 실제 값 그대로 두고 **0 선**을 그어 어디부터가 손실인지 보여줍니다
- **좋아짐/나빠짐 판정은 하지 않습니다.** 남의 회사 매출이 는 것이 우리에게
  좋은지 나쁜지는 화면이 정할 일이 아닙니다. 증감은 회색으로만 보여줍니다

### 매출과 영업이익을 같이 보는 법

둘을 **꺾은선 한 판에 겹치지 않습니다.** 매출 2조 5천억과 영업이익 500억은
자릿수가 쉰 배 차이라 한 축에 얹으면 영업이익이 바닥에 붙은 직선이 되고, 좌우 두
축으로 나누면 두 선이 만나는 자리가 아무 뜻도 없는데 뜻이 있는 것처럼 보입니다.

| 자리 | 보여주는 것 |
|---|---|
| 숫자 요약 한 줄 | 매출액을 크게, 그 아래 작게 **영업이익 · 이익률** |
| 그래프 — **막대**(기본) | 한 막대가 한 회사의 한 기간. 옅은 막대가 매출액, **그 안에 진하게 겹친 막대가 영업이익**. 회사는 기간마다 옆으로 나란히 |
| 그래프 — **선** | **위아래 두 칸**(매출액 / 영업이익), x축(기간)만 함께 |
| 표·엑셀 | 두 지표를 **나란한 열**로 붙여 한 장에 |

**왜 이익 막대를 옆에 세우지 않고 안에 겹치나.** 영업이익은 매출의 2~8%입니다.
옆에 나란히 세우면 이익 막대가 바닥에 깔린 선이 되어 아무것도 안 보입니다. 안에
겹치면 뜻도 어긋나지 않습니다 — 영업이익은 매출에서 남은 몫이라 "이 매출 중
이만큼이 남았다"로 읽힙니다.

**막대는 반드시 0에서 올라옵니다.** 밑을 잘라 그리면 2%와 8%가 두 배쯤 차이 나는
것처럼 보입니다. 막대는 지수(=100)로 바꾸지
않습니다 — 높이가 아무 뜻도 없어지고 이익을 안에 겹칠 수도 없습니다. 그래서
덩치가 크게 다른 회사를 견줄 때는 **선**이 낫고, 매출과 이익을 한 판에서 볼 때는
**막대**가 낫습니다. 두 모양을 다 두는 이유가 이것입니다.

선의 위아래 두 칸은 같은 자리가 같은 기간이라, 눈으로 세로로 훑으면 "매출은
늘었는데 이익은 줄었다"가 바로 보입니다. 칸마다 제 눈금과 제 커서를 갖습니다.

모양 고르개는 **그래프를 펼쳤을 때만** 그래프 바로 위에 붙습니다. 머리줄에
고르개를 셋씩 늘어놓으면 정작 부문 이름이 밀립니다. 보기를 바꾸면 모양은 그
보기의 기본으로 돌아갑니다(매출·이익 → 막대, 지표 하나 → 선).

### 영업이익이 마이너스일 때

매출 눈금 위에서 -217억은 머리카락 굵기입니다. 막대만으로는 적자인지 이익이 0에
가까운 건지 분간이 안 됩니다. 그래서 네 겹으로 말합니다.

1. **0선 아래 적자 구역** — 옅은 붉은 띠를 깔고 오른쪽에 「적자」라고 적습니다.
   띠가 있다는 것 자체가 "이 그림 어딘가에 적자가 있다"는 말이고, 거기 내려간
   막대가 적자입니다. **띠의 깊이는 자료가 아니라 자리입니다** — 막대는 실제 값
   그대로 그리고, 띠만 눈에 보일 만큼(그림 높이의 8% 남짓) 잡아 둡니다. 위쪽에
   여백을 두는 것과 같은 종류의 여백입니다
2. **적자 막대는 최소 두께**를 줍니다. 아주 얇으면 아예 안 보이는데, 안 보이는
   적자는 없는 적자와 같습니다
3. **숫자 요약**은 영업이익·이익률을 붉게 적고 「적자」딱지를 붙입니다. 마이너스
   부호 하나는 숫자가 빽빽한 줄에서 너무 쉽게 지나칩니다
4. **크게 보려면 「영업이익」만** 고르면 됩니다. 그때는 눈금이 이익 하나짜리라
   적자가 제 크기로 — 한샘처럼 내내 적자면 그림의 거의 전부가 — 보입니다

「적자」는 좋다/나쁘다 판정이 아니라 사실이라 씁니다. 남의 회사 매출이 는 것이
우리에게 좋은지 나쁜지는 여전히 화면이 정하지 않습니다.

막대 눈금은 **어림수**로 맞춥니다(64,200 → 66,000). `-5,545.9억원` 같은 수가 축에
적히면 그게 누가 실제로 낸 손실인 줄 읽습니다.

### 분기는 전년 같은 분기와 견준다

분기 증감은 **직전 분기가 아니라 전년 동기**와 견줍니다. 건자재는 겨울에 덜 팔리고
봄에 더 팔려서, 3분기를 2분기와 견주면 계절이 만든 차이를 실적이 움직인 것으로
읽게 됩니다. 전년 같은 분기가 없을 때만 직전 칸으로 물러섭니다.

분기 기간은 `YYYYQn` 한 가지 꼴로만 받습니다. 글자 크기순 정렬이 곧 시간순이라
날짜로 바꾸는 일이 없습니다. 화면 축에는 `25.3Q`로 줄여 적습니다 — 눈금이 예닐곱
개라 `2025 3분기`로 적으면 서로 겹쳐 아무것도 안 읽힙니다.

### 조회 웹훅이 돌려줘야 하는 모양

`lxgroup-경쟁사` 가 이 모양으로 답하면 화면이 그립니다.

```json
{ "ok": true, "asOf": "2026-09-03",
  "짝없는부문": ["도료", "실리콘"],
  "실적": [
    { "회사명": "LX하우시스", "corp_code": "00759513",
      "계열사": ["하우시스"], "부문": "건축자재",
      "우리편": true, "단위": "억원", "근거": "사업보고서 (2025.12)",
      "해": [ { "연도": "2024", "매출액": 25336, "영업이익": 501,
                "수출": 6051, "내수": 19285 } ],
      "분기": [ { "기간": "2025Q3", "매출액": 6300, "영업이익": 160 } ] } ] }
```

`분기`는 없어도 됩니다 — 없으면 그 부문의 「분기별」 단추가 흐려질 뿐 나머지는
그대로 돕니다. `기간`은 반드시 `YYYYQn` 꼴입니다.

부문이 빈 문자열이면 화면에서 「전사」로 묶습니다(판토스처럼 단일영업부문인 회사).

## 시장지표 — 화면 짜임

**하위 탭**이 위 본문 탭과 같은 모양으로 한 줄 더 붙습니다. 공통 / 인터 /
판토스 / 하우시스 / 글라스 / 세미콘 / MMA. 처음 열면 **공통만** 보이고, 탭을
누르면 그 계열사 지표만 나옵니다.

탭 목록은 화면이 들고 있습니다(`IND_TABS`). 시트에 아직 지표가 없는 계열사도
자리를 잡아 두는데, 빈 탭이 보이는 편이 '아직 안 붙었다'를 말해 주고 나중에
시트에 넣으면 그대로 차기 때문입니다. **시트에 우리가 모르는 소속이 생기면 탭을
하나 자동으로 붙입니다** — 안 그러면 그 지표가 어느 탭에도 안 나와 조용히
사라집니다. 시트의 이름이 더 길어도 됩니다(`인터내셔널` → `인터` 탭). 앞머리로
맞춥니다.

### 그래프와 표

블록 오른쪽 위에 **`그래프`**와 **`표`** 두 개가 있고, 서로 **따로** 켜고 끕니다.
둘 다 켜도 되고 표만 켜도 됩니다. 그래프는 '어디쯤에서 꺾였나'를 보여주고 표는
'그게 정확히 얼마였나'를 보여줘서, 서로를 대신하지 못합니다.

**처음에는 둘 다 접혀 있습니다.** 숫자 요약만 보고 지나가는 것이 대부분이고,
여섯 블록이 한꺼번에 펼쳐져 있으면 정작 요약을 찾기가 어렵습니다. 펼침은
기간·통화와 마찬가지로 **블록마다 따로** 기억합니다.

**차례는 그래프를 따릅니다.** 그래프가 왼쪽에서 오른쪽으로 옛것에서 최신으로
가므로, 표도 위에서 아래로 옛것에서 최신으로 갑니다. 한 블록 안에서 둘의 방향이
반대면 같은 자료를 볼 때마다 머리로 뒤집어야 합니다.

대신 **표를 열면 맨 아래로 내려 둡니다.** 스물넉 달을 옛것부터 보여주면 첫 화면이
두 해 전이라 아무 쓸모가 없습니다. 위로 밀면 과거로 가는 것이, 그래프를 왼쪽으로
훑는 것과 같은 몸짓입니다.

기간 칸과 머리글은 스크롤해도 붙어 있습니다 — 스무 줄쯤 내려가면 이 숫자가 어느
달 어느 지표인지 알 길이 없어집니다.

### 엑셀 내려받기

표 아래 `엑셀 내려받기`가 **지금 보고 있는 그대로**(기간·통화·차례) `.xlsx`를
만듭니다. 화면과 파일이 다르면 어느 쪽이 맞는지 매번 확인하게 됩니다. 시계열을
옛것부터 두는 것은 엑셀에서 그래프를 그릴 때도 맞는 차례입니다.

**바깥 라이브러리를 쓰지 않습니다.** 이 저장소는 의존성 없는 정적 HTML이고,
CDN을 하나 걸면 그 CDN이 죽는 날 이 기능도 같이 죽습니다. xlsx는 정해진 XML
다섯 장을 zip으로 묶은 것이라 직접 만들 수 있습니다(`xlsx()` · `zip()` ·
`crc32()`). 압축은 안 합니다(stored) — 스물몇 줄짜리 표라 줄어들 것도 없고,
압축을 넣으면 구현할 것이 훨씬 많아집니다.

값은 **숫자로** 넣습니다. 글자로 넣으면 엑셀에서 그래프도 못 그리고 합계도 안
됩니다. 빈 칸은 그냥 비워 둡니다 — `0`을 넣으면 '그 달에 0'으로 읽혀서 없는
것과 뜻이 달라집니다.

`g`(그래프 데이터)는 **표만 켜도** 만들어집니다. 표가 그 값을 쓰기 때문입니다.
그래서 그래프를 그릴지는 `g`가 있느냐가 아니라 그래프 토글이 켜졌느냐로
정해야 합니다 — 처음에 `if (g)`로 뒀더니 표만 켰는데 그래프가 같이 나왔습니다.

### 선을 가르는 법

한 그래프에 여러 나라가 섞이면 붉은 농담만으로는 못 읽습니다. 한국 기준금리·
미국 기준금리·한국 국채·미국 국채 네 선이 다 같은 실선이면 매번 범례를 다시
봐야 합니다. 그래서 **나라는 색으로, 항목은 선 모양으로** 가릅니다.

| | 기준금리 | 국채 3년 |
|---|---|---|
| **한국** | 붉은 실선 | 붉은 파선 |
| **미국** | 남색 실선 | 남색 파선 |

나라는 **표시명 앞머리**로 압니다(`한국 …` / `미국 …`). 지표ID로 가르는 방법도
있지만 국고채는 `RATE_KTB3`, 미국 국채는 `RATE_US_T3`라 ID만으로는 짝이 안
맞습니다. 그래서 시트의 `국고채 3년`을 **`한국 국채 3년`**으로 고쳤습니다 —
이름은 사람이 보는 것이라 짝이 어긋나면 눈에 띄고, 그래서 안 어긋납니다.

나라가 둘 이상이고 **모든** 지표가 나라를 밝힐 때만 이 방식을 씁니다. 하나라도
모르면 절반만 색이 바뀌어 더 헷갈리므로 통째로 안 씁니다(그때는 붉은 농담).
선 모양은 **기간이나 펼침과 무관하게** 지표 목록만 보고 정합니다 — 그래프를
펼칠 때 색이 바뀌면 같은 지표를 다시 찾아야 합니다.

### 커서를 올리면

세로 안내선과 함께 그 시점의 **날짜와 각 선의 실제 값**이 뜹니다. 지수로 맞춘
그래프(`= 100`)여도 실제 값을 보여줍니다 — 100 언저리 숫자는 읽는 사람에게 아무
뜻이 없습니다. 선을 하나하나 맞히게 하지 않고 세로로 훑습니다.

치우는 일은 `pointerleave` **한 곳에서만** 합니다. 처음에 캡처 단계로 받았더니
커서가 그래프 안으로 들어가면서 다른 자식에서 떠난 것까지 잡혀 **그린 즉시
지워졌습니다.** 상자가 아예 안 뜨는 것처럼 보입니다.

### x축 눈금

처음과 끝만 있으면 가운데가 언제인지 감이 안 오고, 칸마다 다 적으면 글자가 겹쳐
아무것도 안 읽힙니다. **넓은 화면 6개, 좁은 화면(560px 미만) 4개**로 고르게
세웁니다. 마지막 칸은 반드시 넣되 바로 앞 눈금과 붙으면 그 앞것을 뺍니다.

## 시장지표 — 기간 세 가지

그래프마다 오른쪽 위에서 **일별(30일) / 월별(24개월) / 연도별(전체)** 을 고릅니다.
어디까지 보여줄지는 웹훅이 잘라서 보내므로 화면은 자르지 않습니다 — 화면에서
자르기 시작하면 '전일 대비'와 '전월 대비'를 화면이 다시 계산해야 하고, 그 순간
같은 숫자가 두 곳에서 나옵니다.

### 연도별을 만드는 두 가지 방법

지표마다 다르고, `시장지표` 시트의 **`합산방식`** 칸이 정합니다.

| 합산방식 | 연도별 값 | 어떤 지표 |
|---|---|---|
| `마지막값` (기본, 빈칸) | 그 해 마지막 관측 | 환율·금리·단가·지수 — 수준을 보는 것 |
| `합계` | 그 해 **열두 달의 합** | 수출액·수주액·거래량 — 한 달치가 쌓여 한 해를 이루는 것 |

처음에는 전부 마지막값이었습니다. 환율·금리만 있을 때는 맞았는데, 금액 지표가
들어오면서 깨졌습니다. 디스플레이 패널 수출액 `2025년 1,339 백만$`는 그 해
12월 한 달치인데 **연도 라벨 옆에 붙으면 한 해치로 읽힙니다.** 실제 2025년
수출은 15,437 백만$로 **12배** 차이입니다.

**합계인 지표는 열두 달이 다 찬 해만 넣습니다.** 일곱 달만 더한 값을 열두 달과
나란히 놓으면 반토막 난 것처럼 보이는데, 실제로는 아직 안 끝난 것뿐입니다.
그래서 연도별 화면에 올해는 안 나옵니다. 화면 머리에 그렇게 적혀 있습니다.

**미국 세 개(주택 인허가·착공·비주거 건설지출)는 합계가 아닙니다.** FRED가 이미
연율로 환산해 주기 때문입니다(SAAR). `1,433천호`는 그 달 인허가가 아니라 "이
속도면 한 해에 143만 호"라는 뜻이라, 더하면 열두 배가 됩니다.

맞는지는 아는 값으로 검산했습니다 — 주택 매매거래량 2020년 **1,279,305호**
(역대 최고), 2022년 508,790호(급락), 주택 착공 2023년 246,265호.

`시장지표값` 시트에는 **`주기`** 칸이 있습니다.

| 주기 | 무엇 | 어디에 쓰나 |
|---|---|---|
| `일` | 최근 400일치 관측 그대로 | 일별 화면 |
| `월` | 달마다 마지막 관측 하나 | 월별·연도별 화면 |
| (빈칸) | 2026-09-03 이전에 넣은 월말값 | `월`로 친다 |

일별을 400일로 끊는 이유는 대시보드를 열 때마다 시트 전부를 읽기 때문입니다.
30일을 보여주는 데 16년치 일별은 필요 없습니다. 월별은 전부 남깁니다 —
그래야 연도별이 의미가 생깁니다.

**지표마다 고를 수 있는 기간이 다릅니다.** 한국은행이 기준금리·주택담보대출
금리를 일 단위로 내주지 않아 그 그래프는 일별 버튼이 비활성입니다. 한 그래프
안에서 일부만 일별이 있으면(국고채는 있고 기준금리는 없음) 없는 쪽은 목록에
흐리게 남고 선만 빠집니다 — 지우면 "이 지표가 어디 갔지"가 됩니다.

### 달러 지표를 원화로 보기

달러로 매기는 원자재(`$/t`, `$/bbl`)는 그래프마다 **달러 / 원화**를 고를 수
있습니다. 달러값만 봐서는 실제로 얼마를 더 내는지 알 수 없기 때문입니다.

환율은 화면이 따로 들고 있지 않고 **지표 목록에 이미 들어 있는 원/달러
환율(`FX_USDKRW`)을 그대로 씁니다.** 화면에 환율을 하나 더 두면 그래프에
그려지는 환율과 계산에 쓰는 환율이 언젠가 갈라집니다.

세 가지가 중요합니다.

- **기간마다 제 기간의 환율을 씁니다.** 월별은 그 달, 연도별은 그 해입니다.
  최신값과 이전값도 각자 제 기간의 환율로 바꿉니다 — 둘 다 오늘 환율로
  바꾸면 환율이 움직인 몫이 통째로 사라져서, 원화로 봐도 달러와 똑같은
  변화율이 나옵니다.
- **긍정/부정 판정이 뒤집힐 수 있습니다.** 판유리 수출단가는 2026-07에 달러로
  +0.71%인데 원화로는 −5.9%입니다. 그대로 두면 화살표는 아래인데 글자는
  '긍정'인 줄이 나오므로, 변화의 부호가 바뀌면 판정도 함께 뒤집습니다.
  (판정을 담은 칸 이름은 서버가 보내는 `좋아짐` 그대로이고, 화면에 쓰는
  말만 긍정/부정입니다.)
- **환율이 없는 기간의 점은 그리지 않습니다.** 환율 월별이 24개월이라
  원자재의 가장 오래된 한 달이 빠질 수 있습니다. 없는 환율을 지어내는
  것보다 한 점 짧은 편이 낫습니다.

`M$`(미국 비주거 건설지출)처럼 달러로 읽는 것이 자연스러운 지표는 단위가
`$`로 시작하지 않아 이 고르개가 붙지 않습니다. 한 그래프 안이 **전부** 달러일
때만 붙습니다 — 섞여 있으면 반만 바꾼 그래프가 됩니다.

### ECOS를 부를 때

한 번에 **1000행**까지만 줍니다. 환율 일별은 2010년부터만 해도 4119행이라
한 번에 못 받습니다. 그래서 같은 지표를 행 범위만 바꿔 여러 번 부릅니다 —
반복문이 아니라 **항목을 여러 개 내보내는 것**으로 풉니다(n8n은 항목마다 HTTP
노드를 한 번씩 돌립니다). 범위를 넘어간 쪽은 `INFO-200 해당하는 데이터가
없습니다`가 오는데, 그건 오류가 아니라 끝났다는 뜻이라 조용히 버립니다.

환율·국고채는 **월 주기를 아예 제공하지 않습니다**(일 주기만). 그래서 월값은
일별에서 달마다 마지막 관측을 뽑아 만듭니다.

### 판토스 — 물동량으로 본다

컨테이너 운임지수(SCFI·BDI)는 무료 API가 없습니다. 유료 계약 없이 물류를 보는
길은 **물동량** 쪽입니다. 넷 다 한국은행(ECOS)이라 이미 붙은 출처로 됩니다.

| 지표 | 통계표 | 항목 | 단위 |
|---|---|---|---|
| 수출 총액 | `901Y118` 수출입 총괄 | `T002` | 억$ (천불 × 1e-5) |
| 수입 총액 | `901Y118` | `T004` | 억$ |
| 수출물량지수 | `403Y002` | `*AA` 총지수 | 2020=100 |
| 수입물량지수 | `403Y004` | `*AA` 총지수 | 2020=100 |

**금액과 물량을 같이 놓는 이유**가 있습니다. 금액은 가격과 물량이 섞여 있어
"진짜로 많이 실었는지"를 못 말해 줍니다. 물류는 물량이 매출입니다.

총지수 항목코드가 **`*AA`로 별표가 들어갑니다.** 주소 경로에 그대로 넣어도
통하는 것을 실제 호출로 확인했습니다.

ECOS 수출입 총액은 **천불**로 옵니다. 그래서 `scale`을 물렸는데, 배율을
관세청·KOSIS만 쓰고 있어서 **배율을 공통 자리로 올렸습니다.** 단위를 시트가
정하게 해놓고 출처마다 따로 처리하면 한 곳을 반드시 빼먹습니다.

### FRED를 부를 때

한 번에 다 줍니다. 쪽을 나눌 필요가 없습니다. 다만 **응답 어디에도 어느
계열인지가 안 적혀 있습니다.** 그래서 몇 번째 응답이 어느 지표인지를 순서로만
알 수 있고, **지표당 딱 한 번만 불러야** 순서가 안 어긋납니다.

값이 `"."` 로 오는 날이 있습니다 — 휴일이라 관측이 없다는 뜻이니 버립니다.
월간 계열도 날짜가 `2026-08-01`처럼 일자까지 붙어 옵니다. **그걸 보고 일별로
착각하면 한 달에 한 점짜리 '일별 30일' 그래프가 나옵니다** — 일별로 쌓을지는
응답이 아니라 `시장지표` 시트의 `주기` 칸이 정합니다.

### 관세청(data.go.kr)을 부를 때

세 가지가 다른 곳과 정반대입니다.

1. **인증키를 인코딩하면 안 됩니다.** data.go.kr은 인증키를 인코딩본과
   디코딩본 두 벌로 주는데 시트에 든 것은 디코딩본입니다. 다시 인코딩하면
   `등록되지 않은 서비스키`가 됩니다. ECOS·FRED와 반대입니다.
2. **XML만 줍니다.** `type=json`을 붙여도 무시합니다.
3. **한 번에 1년치까지만** 줍니다(`조회기간은 1년이내`). 그래서 해마다 한 번씩
   부릅니다 — ECOS 쪽과 같은 수법입니다.

응답은 **나라별 · HS10별로 쪼개져** 있고 맨 뒤에 `총계` 줄이 붙습니다. 총계
줄은 버리고 나머지를 달마다 합칩니다.

응답에는 **금액(달러)과 중량(kg)이 같이** 들어 있어 셋 중 무엇이든 만들 수
있습니다. 무엇을 쓸지는 `조회설정JSON`의 `metric`이 정합니다.

| metric | 계산 | 쓰는 곳 |
|---|---|---|
| `단가`(기본) | 그 달 전체 금액 ÷ 전체 중량 × 1000 | 유연탄·팜유·니켈·소다회·판유리 단가 |
| `금액` | 금액 합 × `scale` | 판유리 수출액·수입액 (`scale` 1e-6 → 백만$) |
| `중량` | 중량 합 × `scale` | 아직 안 씀 |

단가는 **반드시 합계끼리** 나눕니다 — 나라별 단가를 평균 내면 물량이 적은
나라가 과하게 반영됩니다. 중량이 kg이라 1000을 곱해 t당으로 맞춥니다.

단위는 짐작하지 말고 확인했습니다. 2025년 HS7005 수출 합이 **9,500만 달러 ·
3만 1천 톤**으로 나와 금액이 달러, 중량이 kg임이 확정됩니다(천달러였다면 연간
950억 달러라 한국 총수출을 넘습니다). 시장지표값은 한 번 들어간 줄을 고치지
않으므로, 단위를 틀리면 그 값이 그대로 굳습니다.

공표는 **매달 15일쯤**입니다. 그래서 18일에 받습니다.

### KOSIS를 부를 때

표마다 항목코드(`itmId`)와 분류코드(`objL1`~`objL4`)가 다릅니다. **짐작하면
안 됩니다** — 표 번호까지 그럴듯하게 지어낼 수 있고, 그러면 값이 조용히
엉뚱하게 들어옵니다. 순서는 이렇습니다.

1. `statisticsSearch.do` 로 진짜 표 번호(`tblId`)를 찾는다.
2. `statisticsData.do?method=getMeta&type=ITM&loadGubun=1` 로 그 표의
   `itmId`와 `objL1..n` 을 받는다.
3. 실제로 한 번 불러 **값이 상식적인지 눈으로 확인**한 뒤 시트에 적는다.

시트의 `조회설정JSON`에는 그렇게 확인한 값만 들어 있습니다. `scale`은 시트에
적힌 단위로 맞추는 곱수입니다(KOSIS가 백만원으로 주는데 시트는 억원이라
`0.01`). **화면에서 고치지 않고 수집할 때 맞춥니다** — 숫자를 두 곳에서
만지면 어디서 어긋났는지 못 찾습니다.

### 응답을 글자로 받는 이유

`API 부르기` 노드는 `responseFormat: text` 입니다. JSON으로 받게 두면 **KOSIS
처럼 배열을 돌려주는 곳에서 n8n이 배열을 항목 여러 개로 쪼갭니다.** 그러면
'몇 번째 응답이 어느 지표인가'가 통째로 어긋나고, **그 뒤 출처들의 값이 전부
엉뚱한 지표에 붙습니다.** 오류도 안 나고 그래프는 멀쩡해 보입니다.

글자로 받아 `값 정리하기`에서 직접 해석하면 응답 하나가 항목 하나로 유지됩니다.

### HS 코드를 고를 때

**4자리로 먼저 부르고 세부 코드를 봅니다.** 응답이 HS10별로 쪼개져 오므로,
어느 세부 코드가 실제 물량을 갖는지 보고 골라야 합니다. 짐작하면 이름과 값이
어긋난 채로 몇 달이 갑니다.

두 번 걸렸습니다.

| 4자리 | 골라야 할 것 | 같이 든 것 |
|---|---|---|
| `2710` | `2710124000` **나프타** (수입 165억$ @617$/t) | `2710121000`은 우리가 파는 **휘발유** (수출 98억$) |
| `2916` | `2916141000` **MMA** (@1,393~1,502$/t) | `2916149000`은 @2,622~5,616$/t짜리 **특수 메타크릴레이트** |

`2916149000`을 MMA로 붙였으면 값이 두 배로 나오는데 그래프는 멀쩡해 보입니다.

### 아직 못 붙인 지표

| 지표 | 왜 |
|---|---|
| 도시가스(산업용) 요금 | KOSIS에 요금 표가 없습니다. **LNG 수입단가로 대신하고 있습니다** — 요금은 규제가격이라 후행하고, LNG 수입단가가 그 원가 자체입니다. |
| 자동차 생산지수 | 품목별 표가 필요한데 찾은 것은 시도별이라 못 씁니다. |

`수출입 총액`은 풀렸습니다. 관세청 API가 HS 코드를 반드시 요구해 총액을 못
만들었는데, **출처를 한국은행으로 바꾸니 그대로 됩니다**(`901Y118` 수출입
총괄). 막힌 것이 지표가 아니라 출처였던 경우입니다.

판유리는 단가에 더해 **수출액·수입액**(백만$)이 붙어 있습니다. 단가만 보면 값을
더 받았는지 더 팔았는지가 안 갈립니다. 수입 단가는 수출의 8분의 1 수준이라
(2025년 평균 389 $/t 대 3,016 $/t) 수입 물량이 그대로 경쟁 강도입니다 — 싼
범용 판유리가 들어오고 비싼 특수 판유리가 나가는 구조입니다.

OPINET에는 **국제유가 항목이 아예 없습니다.** 열려 있는 것은 국내 주유소
가격뿐이라 두바이유는 FRED(`POILDUBUSDM`)에서 받습니다. `B027`은 두바이유가
아니라 **휘발유**입니다 — 이걸 두바이유로 붙였으면 국내 휘발유 가격에
"두바이유"라는 이름표가 붙은 채로 몇 달이 갔을 것입니다.
