# muwon406 — 작업 규칙

## 글쓰기 규칙 (중요)

사용자와의 대화, 커밋 메시지, 코드 주석, 문서를 **일반적인 한국어 실무 문체**로 쓴다.

- **표준 용어를 그대로 쓴다.** 필터, 정렬, 그룹, 탭, 대시보드, 키워드, 에러, 테스트,
  워크플로우, 스프레드시트, 컬럼, 행, 캐시, 배포.
- **순우리말 신조어를 만들지 않는다.** 아래는 이 저장소에서 실제로 쓰였던 잘못된
  예이며, 앞으로 쓰지 않는다:

  | 쓰지 말 것 | 대신 쓸 것 |
  |---|---|
  | 낱말, 말 | 키워드, 단어 |
  | 거르개, 걸러내기 | 필터 |
  | 고르개 | 선택 버튼, 셀렉터 |
  | 차림표 | 메뉴 |
  | 짜임, 얼개 | 구조 |
  | 말썽 | 오류, 에러 |
  | 칸, 줄 | 열/컬럼, 행 |
  | 단추 | 버튼 |
  | 갈래 | 종류, 구분 |
  | 웹훅을 부른다 | 웹훅을 호출한다 |
  | 자료 | 데이터 |
  | 들머리 | 진입점 |
  | 껍데기 | 빈 화면, 플레이스홀더 |

- 문장은 평이한 설명체로 쓴다. 비유나 잠언조("~것보다 낫다", "~는 일이 아니다")를
  남발하지 않는다.
- 코드 주석은 **왜 그렇게 했는지**를 한두 문장으로 적는다. 길게 늘어놓지 않는다.

> 참고: `README.md`와 `public/lxgroup-mi/hub.html`의 기존 주석 상당수가 위의
> 잘못된 문체로 쓰여 있다. **그 문체를 따라 쓰지 말 것.** 새로 쓰는 글은 이 규칙을
> 따르고, 기존 문서는 손대는 김에 조금씩 고친다.

## 현재 상태와 다음 할 일

`docs/HANDOVER.md`를 먼저 읽는다.

## 안전 규칙

- 이 저장소는 **공개(public)** 다. API 키, 비밀번호, 토큰을 어떤 파일에도 넣지 않는다.
- API 키를 대화에 출력하지 않는다. n8n 설정 시트를 입력으로 받는 Code 노드에서
  **절대 `throw` 하지 않는다** — n8n이 에러에 노드 입력을 첨부해 키가 노출된다.
  대신 진단용 아이템을 return 한다.
- `LXGroup_지표수집`, `LXGroup_경쟁사_공시수집`은 `saveDataSuccessExecution: none`
  으로 둔다(출력 URL에 키가 들어감).
- 라이브 n8n 워크플로우를 고칠 때는 **publish 전에 draft를 수동 실행해 확인**한다.
  `addNode`는 `executeOnce`/`alwaysOutputData`를 무시하므로, 노드를 추가한 뒤
  `setNodeSettings`로 따로 지정한다.
